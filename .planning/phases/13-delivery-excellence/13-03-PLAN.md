---
phase: 13-delivery-excellence
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - automation/n8n-workflows/TG-88-on-my-way-sms.json
  - automation/n8n-workflows/TG-05-jobber-email-parser.json
autonomous: true

must_haves:
  truths:
    - "TG-88 on-my-way SMS workflow is ACTIVE on n8n"
    - "TG-88 sends SMS when TG-05 routes a visit_confirmed event to it"
    - "TG-88 Supabase API keys use correct placeholder format (not stale {{SUPABASE_SECRET}} mustache)"
    - "TG-88 Resend API key uses correct placeholder format"
    - "TG-05 Call TG-88 node has correct workflow ID matching the live n8n instance"
  artifacts:
    - path: "automation/n8n-workflows/TG-88-on-my-way-sms.json"
      provides: "On-my-way SMS sub-workflow with fixed credentials"
      contains: "SUPABASE_SECRET_KEY"
  key_links:
    - from: "TG-05 Route Event Type visit_confirmed output"
      to: "TG-88 via executeWorkflow"
      via: "executeWorkflow with correct workflowId"
      pattern: "call-tg88"
    - from: "TG-88 Send On My Way SMS"
      to: "Twilio API"
      via: "n8n-nodes-base.twilio with credential Qd6M2hA9hDWRKGPR"
      pattern: "twilioApi"
---

<objective>
Fix stale credential references in TG-88 on-my-way-sms workflow, verify TG-05's Call TG-88 node has the correct workflow ID, and activate TG-88 on the n8n instance.

Purpose: DELV-05 -- TG-88 exists but is INACTIVE. It has stale `{{SUPABASE_SECRET}}` mustache-style placeholders and a Resend auth header using `{{SUPABASE_SECRET}}` (wrong key entirely). Fix these issues and activate so visit_confirmed events from TG-05 trigger on-my-way SMS.

Output: Fixed TG-88 JSON deployed and active on n8n, TG-05 Call TG-88 node verified with correct workflow ID.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-delivery-excellence/13-01-SUMMARY.md
@automation/n8n-workflows/TG-88-on-my-way-sms.json
@automation/n8n-workflows/TG-05-jobber-email-parser.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TG-88 stale credential references and modernize</name>
  <files>automation/n8n-workflows/TG-88-on-my-way-sms.json</files>
  <action>
The existing TG-88 has several issues that need fixing:

1. **Stale Supabase key placeholders**: Multiple nodes use `{{SUPABASE_SECRET}}` mustache-style which is a stale pattern. Replace ALL occurrences with the correct format used in active workflows:
   - apikey header: `"sb_publishable_DeX21ldoDKl3NyJeFZzR1w_eWBUkx-v"` (anon key -- this is the public key, safe in workflows)
   - Authorization header: `"Bearer SUPABASE_SECRET_KEY"` (placeholder for service key)

   Nodes to fix:
   - "Check SMS Consent" (id: tg88-check-consent) -- both apikey and Authorization headers
   - "Log SMS Send" (id: tg88-log-send) -- both apikey and Authorization headers
   - "Update Send Status" (id: tg88-update-status) -- both apikey and Authorization headers

2. **Stale Resend API key**: The "Send On My Way Email" node (id: tg88-email-fallback) has `Authorization: {{SUPABASE_SECRET}}` which is completely wrong -- it should be `Authorization: Bearer RESEND_API_KEY` (placeholder). Fix this.

3. **Consent check may fail**: The `can_send_sms` RPC function may not exist in Supabase. Set continueOnFail: true on the "Check SMS Consent" node (id: tg88-check-consent) so the workflow degrades gracefully. If consent check fails, default to sending (better to over-communicate than miss an on-my-way notification).

4. **Add continueOnFail**: Also verify "Has SMS Consent?" If node (id: tg88-if-consent) -- if the consent check returns an error object instead of a boolean, the If node may route incorrectly. Add a fallback: if the consent check response is not a boolean true/false, default to the "send" path. This can be done by adjusting the If condition to check for `$json !== false` rather than `$json === true`.

5. **Verify Twilio credential**: The Send On My Way SMS node (id: tg88-send-sms) correctly references credential id `Qd6M2hA9hDWRKGPR` (TG - Twilio). This is correct -- do NOT change it.

6. **SMS message improvement**: Update the SMS message in both "Send On My Way SMS" and "Log SMS Send" nodes to be more concise:
   `Hi {name}! TotalGuard crew is heading to {address} now. ETA ~15-30 min. Gates unlocked & pets secured? Questions: 608-535-6057`

Save the updated JSON locally.
  </action>
  <verify>
1. Grep TG-88 JSON for `{{SUPABASE_SECRET}}` -- should return 0 matches
2. Check "Send On My Way Email" Authorization header contains "RESEND_API_KEY" not "SUPABASE_SECRET"
3. Check SMS Consent node has continueOnFail: true
4. Twilio credential id is still Qd6M2hA9hDWRKGPR
5. Updated SMS message text is present
  </verify>
  <done>TG-88 JSON has all credential references fixed, consent check degrades gracefully, SMS copy improved</done>
</task>

<task type="auto">
  <name>Task 2: Deploy TG-88 to n8n, activate, and verify TG-05 routing</name>
  <files>
    automation/n8n-workflows/TG-88-on-my-way-sms.json
    automation/n8n-workflows/TG-05-jobber-email-parser.json
  </files>
  <action>
1. **Check TG-88 on n8n**: GET `https://tgyardcare.app.n8n.cloud/api/v1/workflows/2IB0qFPgBm0YxNtP` to see if TG-88 exists on the live instance. The local JSON has id "2IB0qFPgBm0YxNtP".

2. **Deploy TG-88**: If it exists, PUT the updated JSON to update it. If not, POST to create it.
   - PUT `https://tgyardcare.app.n8n.cloud/api/v1/workflows/2IB0qFPgBm0YxNtP`
   - Use the n8n API key from reference_n8n.md

3. **Activate TG-88**: POST `https://tgyardcare.app.n8n.cloud/api/v1/workflows/2IB0qFPgBm0YxNtP/activate`
   - Verify response shows active: true

4. **Verify TG-05 Call TG-88 node**: Check that the "Call TG-88 On My Way" node in TG-05 has workflowId "2IB0qFPgBm0YxNtP". From the existing JSON, the Call TG-88 node already has this ID. Confirm it matches by reading the live TG-05 from n8n.

   If the TG-88 n8n ID is different from "2IB0qFPgBm0YxNtP" (e.g., if it was recreated during deploy), update TG-05's Call TG-88 node workflowId and PUT TG-05 back to n8n.

5. **Final verification**: GET both workflows from n8n API and confirm:
   - TG-88 active: true
   - TG-05 active: true
   - TG-05 Call TG-88 node workflowId matches TG-88's actual n8n ID
  </action>
  <verify>
1. GET TG-88 from n8n returns active: true
2. GET TG-05 from n8n shows Call TG-88 node with correct workflowId
3. TG-88 node list includes "Send On My Way SMS" with Twilio credential
4. No `{{SUPABASE_SECRET}}` in the live workflow
  </verify>
  <done>TG-88 deployed, active on n8n, and correctly wired from TG-05 visit_confirmed output. On-my-way SMS will fire when Jobber sends visit confirmation emails.</done>
</task>

</tasks>

<verification>
1. TG-88 active on n8n (GET returns active: true)
2. TG-88 has no stale credential references
3. TG-05 visit_confirmed output routes to TG-88 with correct workflow ID
4. TG-88 Twilio credential is Qd6M2hA9hDWRKGPR (TG - Twilio)
5. TG-88 SMS consent check degrades gracefully on failure
</verification>

<success_criteria>
- TG-88 is ACTIVE on n8n instance
- Visit confirmed emails from Jobber trigger on-my-way SMS to customer
- SMS includes property address and ETA
- Email fallback works when customer has no phone but has email
- No stale credential references remain in TG-88
</success_criteria>

<output>
After completion, create `.planning/phases/13-delivery-excellence/13-03-SUMMARY.md`

Include in SUMMARY:
- TG-88 n8n workflow ID (confirmed)
- Whether TG-05 Call TG-88 workflowId needed updating
- Total active workflow count after this plan
- Note about Twilio A2P 10DLC status (all SMS may be carrier-filtered)
- Note about can_send_sms RPC function potentially not existing in Supabase
</output>
