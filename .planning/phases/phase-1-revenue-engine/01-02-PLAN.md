---
phase: 01-revenue-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n workflow TG-05 (updated via n8n API)
autonomous: true

must_haves:
  truths:
    - "TG-05 polls every 15 minutes instead of 5 (execution budget fix)"
    - "TG-05 Parse Jobber Email code also detects quote_sent, visit_confirmed, and plan_accepted events"
    - "TG-05 Switch node routes quote_sent, invoice_sent, visit_confirmed, and plan_accepted to Execute Sub-workflow nodes"
    - "All $vars references in TG-05 are replaced with hardcoded values"
    - "TG-05 remains functional for existing new_request and job_completed routes"
  artifacts: []
  key_links:
    - from: "TG-05 Route Event Type"
      to: "TG-83, TG-84, TG-86, TG-88, TG-89"
      via: "Execute Sub-workflow nodes"
      pattern: "executeWorkflow"
---

<objective>
Update TG-05 (Jobber Email Parser) to: (1) fix all $vars references with hardcoded values, (2) reduce poll frequency from 5min to 15min to stay within n8n execution budget, (3) add quote_sent, visit_confirmed, and plan_accepted detection to the parser code, (4) add 4 new routes to the Switch node for quote_sent, invoice_sent, visit_confirmed, and plan_accepted, and (5) add Execute Sub-workflow nodes that will call TG-83, TG-84, TG-86, TG-88, TG-89 once those workflows are created.

Purpose: TG-05 is the central event router. All Jobber-triggered workflows depend on it routing events correctly.
Output: Updated TG-05 workflow deployed to n8n cloud.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-1-revenue-engine/01-RESEARCH.md
@tgyardcare/automation/n8n-workflows/TG-05-jobber-email-parser.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch current TG-05 from n8n and update it</name>
  <files>n8n workflow TG-05 (via API)</files>
  <action>
Step 1: List all workflows from n8n to find TG-05's workflow ID:
```bash
curl -s -H "X-N8N-API-KEY: $N8N_API_KEY" \
  "https://tgyardcare.app.n8n.cloud/api/v1/workflows?limit=100" | jq '.data[] | select(.name | test("TG-05"; "i")) | {id, name, active}'
```

Step 2: GET the full workflow JSON by ID:
```bash
curl -s -H "X-N8N-API-KEY: ..." "https://tgyardcare.app.n8n.cloud/api/v1/workflows/{ID}"
```

Step 3: Update the workflow with these changes (use PUT to replace the entire workflow):

**Change 1 - Poll frequency:** In the IMAP trigger node, change `"value": 5` to `"value": 15` (15-minute polling = 2,880/month instead of 8,640/month).

**Change 2 - Fix all $vars references:** Replace ALL occurrences:
- `={{$vars.TG_SUPABASE_URL}}` -> `=https://lwtmvzhwekgdxkaisfra.supabase.co`
- `={{$vars.TG_SUPABASE_ANON_KEY}}` -> `=SUPABASE_ANON_KEY`
- `={{$vars.TG_SUPABASE_SERVICE_KEY}}` -> `=SUPABASE_SERVICE_KEY`
- `={{$vars.TG_BREVO_API_KEY}}` -> `=BREVO_API_KEY`
- `={{$vars.TG_TWILIO_PHONE_NUMBER}}` -> `=+16089953554`
- Any other `$vars` references found

**Change 3 - Extend parser code:** In the "Parse Jobber Email" Code node, add detection for new event types to the JavaScript. Add these lines to the event_type detection block:
```javascript
else if (/quote|estimate/i.test(subject)) event_type = 'quote_sent';
else if (/on (my|the) way|visit confirmed|appointment confirmed|en route/i.test(subject)) event_type = 'visit_confirmed';
else if (/accepted|approved|plan confirmed/i.test(subject)) event_type = 'plan_accepted';
```

**Change 4 - Extend Switch node:** Add 4 new rules to the "Route Event Type" Switch node's `rules.rules` array:
```json
{
  "conditions": {
    "conditions": [
      {
        "leftValue": "={{ $json.event_type }}",
        "rightValue": "quote_sent",
        "operator": { "type": "string", "operation": "equals" }
      }
    ],
    "combinator": "and"
  },
  "renameOutput": true,
  "outputKey": "quote_sent"
},
{
  "conditions": {
    "conditions": [
      {
        "leftValue": "={{ $json.event_type }}",
        "rightValue": "invoice_sent",
        "operator": { "type": "string", "operation": "equals" }
      }
    ],
    "combinator": "and"
  },
  "renameOutput": true,
  "outputKey": "invoice_sent"
},
{
  "conditions": {
    "conditions": [
      {
        "leftValue": "={{ $json.event_type }}",
        "rightValue": "visit_confirmed",
        "operator": { "type": "string", "operation": "equals" }
      }
    ],
    "combinator": "and"
  },
  "renameOutput": true,
  "outputKey": "visit_confirmed"
},
{
  "conditions": {
    "conditions": [
      {
        "leftValue": "={{ $json.event_type }}",
        "rightValue": "plan_accepted",
        "operator": { "type": "string", "operation": "equals" }
      }
    ],
    "combinator": "and"
  },
  "renameOutput": true,
  "outputKey": "plan_accepted"
}
```

**Change 5 - Add Execute Sub-workflow nodes:** Add these 5 new nodes (positions offset below existing nodes):

Node: "Call TG-83 Quote Follow-up"
```json
{
  "parameters": {
    "workflowId": "={{ '' }}",
    "options": {}
  },
  "id": "call-tg83",
  "name": "Call TG-83 Quote Follow-up",
  "type": "n8n-nodes-base.executeWorkflow",
  "typeVersion": 1,
  "position": [1100, 200],
  "notes": "TODO: Set workflowId after TG-83 is created"
}
```

Node: "Call TG-84 Invoice Collections"
```json
{
  "parameters": {
    "workflowId": "={{ '' }}",
    "options": {}
  },
  "id": "call-tg84",
  "name": "Call TG-84 Invoice Collections",
  "type": "n8n-nodes-base.executeWorkflow",
  "typeVersion": 1,
  "position": [1100, 400],
  "notes": "TODO: Set workflowId after TG-84 is created"
}
```

Node: "Call TG-86 Plan Enrollment"
```json
{
  "parameters": {
    "workflowId": "={{ '' }}",
    "options": {}
  },
  "id": "call-tg86",
  "name": "Call TG-86 Plan Enrollment",
  "type": "n8n-nodes-base.executeWorkflow",
  "typeVersion": 1,
  "position": [1100, 500],
  "notes": "TODO: Set workflowId after TG-86 is created"
}
```

Node: "Call TG-88 On My Way"
```json
{
  "parameters": {
    "workflowId": "={{ '' }}",
    "options": {}
  },
  "id": "call-tg88",
  "name": "Call TG-88 On My Way",
  "type": "n8n-nodes-base.executeWorkflow",
  "typeVersion": 1,
  "position": [1100, 600],
  "notes": "TODO: Set workflowId after TG-88 is created"
}
```

Node: "Call TG-89 Invoice Delivery"
```json
{
  "parameters": {
    "workflowId": "={{ '' }}",
    "options": {}
  },
  "id": "call-tg89",
  "name": "Call TG-89 Invoice Delivery",
  "type": "n8n-nodes-base.executeWorkflow",
  "typeVersion": 1,
  "position": [1320, 400],
  "notes": "TODO: Set workflowId after TG-89 is created"
}
```

**Change 6 - Wire connections:** Add to the connections object:
- Switch output index 2 (quote_sent) -> "Call TG-83 Quote Follow-up"
- Switch output index 3 (invoice_sent) -> "Call TG-84 Invoice Collections" AND "Call TG-89 Invoice Delivery" (fan-out: both receive the event)
- Switch output index 4 (visit_confirmed) -> "Call TG-88 On My Way"
- Switch output index 5 (plan_accepted) -> "Call TG-86 Plan Enrollment"

Add these connection entries:
```json
// In "Route Event Type".main, add outputs at indexes 2, 3, 4, 5:
// Index 2 (quote_sent):
[{ "node": "Call TG-83 Quote Follow-up", "type": "main", "index": 0 }]
// Index 3 (invoice_sent):
[
  { "node": "Call TG-84 Invoice Collections", "type": "main", "index": 0 },
  { "node": "Call TG-89 Invoice Delivery", "type": "main", "index": 0 }
]
// Index 4 (visit_confirmed):
[{ "node": "Call TG-88 On My Way", "type": "main", "index": 0 }]
// Index 5 (plan_accepted):
[{ "node": "Call TG-86 Plan Enrollment", "type": "main", "index": 0 }]
```

Each Execute Sub-workflow node should also connect to "Mark Event Processed":
```json
"Call TG-83 Quote Follow-up": { "main": [[{ "node": "Mark Event Processed", "type": "main", "index": 0 }]] },
"Call TG-84 Invoice Collections": { "main": [[{ "node": "Mark Event Processed", "type": "main", "index": 0 }]] },
"Call TG-86 Plan Enrollment": { "main": [[{ "node": "Mark Event Processed", "type": "main", "index": 0 }]] },
"Call TG-88 On My Way": { "main": [[{ "node": "Mark Event Processed", "type": "main", "index": 0 }]] },
"Call TG-89 Invoice Delivery": { "main": [[{ "node": "Mark Event Processed", "type": "main", "index": 0 }]] }
```

Step 4: PUT the updated workflow JSON back via n8n API:
```bash
curl -s -X PUT -H "X-N8N-API-KEY: ..." -H "Content-Type: application/json" \
  "https://tgyardcare.app.n8n.cloud/api/v1/workflows/{ID}" \
  -d @updated-tg05.json
```

Step 5: Activate the workflow if it was previously active.

IMPORTANT: Also fix $vars references in the "Mark Event Processed" node and "Send Review Request" node -- they both use $vars for Supabase and Brevo URLs.

IMPORTANT: Keep the workflow ACTIVE after update. Do NOT deactivate it during the update process if it was already active.
  </action>
  <verify>
1. GET the workflow via API and confirm:
   - IMAP poll interval is 15 minutes
   - No `$vars` references remain in any node (search for "$vars" in the JSON)
   - Switch node has 6 rules (new_request, job_completed, quote_sent, invoice_sent, visit_confirmed, plan_accepted)
   - 5 Execute Sub-workflow nodes exist (TG-83, TG-84, TG-86, TG-88, TG-89)
   - Parse Jobber Email code includes quote_sent, visit_confirmed, and plan_accepted detection
2. Workflow is active (check `active: true` in API response)
3. Verify existing routes still work: confirm the new_request and job_completed output connections are unchanged from the original workflow
  </verify>
  <done>TG-05 polls every 15 minutes, has zero $vars references, routes 6 event types, and has placeholder sub-workflow calls for TG-83/84/86/88/89. Existing new_request and job_completed routes remain functional.</done>
</task>

<task type="auto">
  <name>Task 2: Update local TG-05 JSON file to match deployed version</name>
  <files>tgyardcare/automation/n8n-workflows/TG-05-jobber-email-parser.json</files>
  <action>
After the n8n deployment succeeds, GET the final workflow JSON from the n8n API and save it to the local file at `tgyardcare/automation/n8n-workflows/TG-05-jobber-email-parser.json`.

This keeps the local repo in sync with what's deployed. Strip any n8n-internal fields (like `updatedAt`, `createdAt`, `versionId`, `id` at the workflow level) -- keep only: name, nodes, connections, settings.
  </action>
  <verify>
Verify the local JSON file has the updated Switch rules and no $vars references:
```bash
grep -c '\$vars' tgyardcare/automation/n8n-workflows/TG-05-jobber-email-parser.json
# Should return 0
grep -c 'quote_sent' tgyardcare/automation/n8n-workflows/TG-05-jobber-email-parser.json
# Should return at least 2
grep -c 'plan_accepted' tgyardcare/automation/n8n-workflows/TG-05-jobber-email-parser.json
# Should return at least 2
```
  </verify>
  <done>Local TG-05 JSON matches the deployed n8n workflow, with zero $vars references and all 6 event routes including plan_accepted.</done>
</task>

</tasks>

<verification>
- TG-05 is live on n8n cloud with 15-minute polling
- All $vars references eliminated
- Switch node has 6 event type routes (new_request, job_completed, quote_sent, invoice_sent, visit_confirmed, plan_accepted)
- 5 Execute Sub-workflow placeholder nodes exist (TG-83, TG-84, TG-86, TG-88, TG-89)
- Existing new_request and job_completed routes remain functional and unchanged
- Local JSON file matches deployed version
</verification>

<success_criteria>
TG-05 is the working event router ready to dispatch to sub-workflows. All existing functionality (new_request, job_completed) continues working. New event types are detected and routed, including plan_accepted which triggers TG-86 Plan Enrollment.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1-revenue-engine/01-02-SUMMARY.md`

CRITICAL: Record the n8n workflow IDs created/updated in the summary. Later plans need the TG-05 workflow ID and will need to backfill the Execute Sub-workflow node workflowId fields.
</output>
