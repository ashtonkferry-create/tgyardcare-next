---
phase: 06-brand-transformation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/ServicesCarousel.tsx
  - src/components/home/BeforeAfterPreview.tsx
  - src/components/home/ServiceStandard.tsx
  - src/components/home/HowItWorks.tsx
autonomous: true

must_haves:
  truths:
    - "ServicesCarousel.tsx is a client island that renders the services grid with embla carousel, navigation dots, and residential/commercial links"
    - "BeforeAfterPreview.tsx is a Server Component that renders the before/after image section with gallery CTA link"
    - "ServiceStandard.tsx is a Server Component that renders the 4-item TotalGuard standard bar with ScrollRevealWrapper for animation"
    - "HowItWorks.tsx is a Server Component that renders the process timeline heading and wraps the client ProcessTimeline component"
  artifacts:
    - path: "src/components/home/ServicesCarousel.tsx"
      provides: "Full services section with carousel, badges, nav dots, and quick links"
      contains: "use client"
    - path: "src/components/home/BeforeAfterPreview.tsx"
      provides: "Homepage before/after teaser with image and CTA to gallery — SERVER COMPONENT"
    - path: "src/components/home/ServiceStandard.tsx"
      provides: "TotalGuard standard section with 4 trust items — SERVER COMPONENT with ScrollRevealWrapper"
    - path: "src/components/home/HowItWorks.tsx"
      provides: "How it works section — SERVER COMPONENT wrapping client ProcessTimeline"
  key_links:
    - from: "src/components/home/ServicesCarousel.tsx"
      to: "src/components/ui/carousel"
      via: "Carousel, CarouselContent, CarouselItem, CarouselApi imports"
      pattern: "CarouselApi"
    - from: "src/components/home/ServicesCarousel.tsx"
      to: "src/lib/seasonalServices"
      via: "sortServicesBySeason for season-aware service ordering"
      pattern: "sortServicesBySeason"
    - from: "src/components/home/ServiceStandard.tsx"
      to: "src/components/home/ScrollRevealWrapper.tsx"
      via: "Wraps static content in client scroll reveal island"
      pattern: "ScrollRevealWrapper"
---

<objective>
Extract the remaining 4 inline sections from HomeContent.tsx into standalone components, with correct server/client split per locked CONTEXT.md decisions.

Purpose: This completes the extraction of all inline sections from the 546-line HomeContent.tsx. ServicesCarousel stays client (needs carousel state). BeforeAfterPreview, ServiceStandard, and HowItWorks become Server Components (static content only). After Plan 01 extracts the top 3 sections and Plan 02 extracts the bottom 4, Plan 03 can rewrite page.tsx as a Server Component.

Output: 4 new component files in `src/components/home/` — 1 client island + 3 server components.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-brand-transformation/06-RESEARCH.md
@.planning/phases/06-brand-transformation/06-CONTEXT.md
@src/app/HomeContent.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ServicesCarousel (client) and BeforeAfterPreview (server) components</name>
  <files>src/components/home/ServicesCarousel.tsx, src/components/home/BeforeAfterPreview.tsx</files>
  <action>
Extract the two largest remaining inline sections from HomeContent.tsx into standalone files. Do NOT modify HomeContent.tsx — just create the new component files.

**ServicesCarousel.tsx** — CLIENT ISLAND (needs carousel state + seasonal context):
- `'use client'` directive
- Must contain its own state: `useState<CarouselApi>` for carouselApi and `useState(0)` for current slide
- Must contain its own `useEffect` for carousel selection tracking
- Move ALL service data (`baseServices`, `allServices`, `services` constant with `sortServicesBySeason`) into this component
- Move ALL static image imports (`mowingImage`, `herbicideImage`, etc.) and the `imgSrc` helper into this component
- Import `useScrollReveal` for `servicesRef`/`servicesInView` and `badgesRef`/`badgesInView`
- Move the `nextdoorBadge` and `satisfactionBadge` imports here
- The component renders the full dark cinematic block: badges, services header, carousel, nav dots, residential/commercial links, and micro-trust line
- Type: `export function ServicesCarousel(): React.JSX.Element`

**BeforeAfterPreview.tsx** — SERVER COMPONENT (static content, no state):
- Do NOT add `'use client'` directive
- Do NOT import Framer Motion (motion.div) directly
- Do NOT import `useScrollReveal`
- Import `Image` from `next/image`, `Link` from `next/link`
- Import `CheckCircle2`, `ArrowRight` from lucide-react (these work in server components)
- Import `ScrollRevealWrapper` from `@/components/home/ScrollRevealWrapper` for the reveal animation
- Renders the "Real Results. Real Properties." section with the transformation image and gallery CTA
- Wrap the content in `<ScrollRevealWrapper>` for animation
- If the existing code uses hover interactions (e.g., hover state on the CTA button), that's fine — CSS hover states work in server components. Only `onClick`, `useState`, `useEffect` require client.
- Type: `export function BeforeAfterPreview(): React.JSX.Element`

CRITICAL: Run `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` after creating files. Must not increase above 84.
  </action>
  <verify>
1. Both files exist in `src/components/home/`
2. `grep "'use client'" src/components/home/ServicesCarousel.tsx` returns a match
3. `grep "'use client'" src/components/home/BeforeAfterPreview.tsx` returns NOTHING (server component)
4. `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` shows no increase from 84
5. No `any` type in new files
  </verify>
  <done>ServicesCarousel.tsx (client) and BeforeAfterPreview.tsx (server) exist and compile without new TS errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ServiceStandard (server) and HowItWorks (server) components</name>
  <files>src/components/home/ServiceStandard.tsx, src/components/home/HowItWorks.tsx</files>
  <action>
Extract the final two inline sections from HomeContent.tsx. Do NOT modify HomeContent.tsx.

**ServiceStandard.tsx** — SERVER COMPONENT (static content, no state):
- Do NOT add `'use client'` directive
- Do NOT import Framer Motion (motion.div) or `useScrollReveal`
- Import `Clock`, `CheckCircle2`, `Users`, `Shield` from lucide-react (server-safe)
- Import `ScrollRevealWrapper` from `@/components/home/ScrollRevealWrapper` for the reveal animation
- Contains the inline standard items array and renders the 4-column grid
- Wrap the grid in `<ScrollRevealWrapper>` for the scroll-triggered animation
- Type: `export function ServiceStandard(): React.JSX.Element`

**HowItWorks.tsx** — SERVER COMPONENT that wraps a client child:
- Do NOT add `'use client'` directive
- Import `ProcessTimeline` from `@/components/ProcessTimeline`
- ProcessTimeline is a client component — importing it from a server component is fine (Next.js handles the boundary automatically)
- Renders the "What Happens When You Contact Us" heading (server-rendered) + ProcessTimeline (client island)
- If ProcessTimeline has `'use client'`, it will hydrate on the client while the heading renders on the server
- Type: `export function HowItWorks(): React.JSX.Element`

CRITICAL: Run `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` after creating files. Must not increase above 84.
  </action>
  <verify>
1. Both files exist in `src/components/home/`
2. `grep "'use client'" src/components/home/ServiceStandard.tsx` returns NOTHING (server component)
3. `grep "'use client'" src/components/home/HowItWorks.tsx` returns NOTHING (server component)
4. `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` shows no increase from 84
5. No `any` type in new files
  </verify>
  <done>All 7 home section components exist in `src/components/home/`. Server/client split: 4 server components (StatsStrip, BeforeAfterPreview, ServiceStandard, HowItWorks), 3 client islands (HeroSection, SeasonalServicesSection, ServicesCarousel), plus ScrollRevealWrapper utility. TypeScript error count unchanged at 84.</done>
</task>

</tasks>

<verification>
1. All 4 new component files exist in `src/components/home/`
2. TypeScript error count remains at 84
3. HomeContent.tsx is still untouched
4. Combined with Plan 01's components, all sections are extracted
5. Server/client split matches CONTEXT.md: ServicesCarousel is client, BeforeAfterPreview/ServiceStandard/HowItWorks are server
</verification>

<success_criteria>
- 4 new components created with correct server/client classification
- ServicesCarousel: client (carousel state + seasonal context)
- BeforeAfterPreview: server (static content, ScrollRevealWrapper for animation)
- ServiceStandard: server (static content, ScrollRevealWrapper for animation)
- HowItWorks: server (wraps client ProcessTimeline)
- Zero new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-transformation/06-02-SUMMARY.md`
</output>
