---
phase: 06-brand-transformation
plan: 06
type: execute
wave: 3
depends_on: ["03", "05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Lighthouse Performance score is 90+ on desktop for the homepage"
    - "Lighthouse Performance score is 75+ on mobile for the homepage"
    - "All 7 homepage JSON-LD scripts match the pre-conversion baseline"
    - "TypeScript error count has not increased above 84"
    - "Homepage view-source contains server-rendered HTML for sr-only section, navigation, and schema scripts"
    - "Server components (StatsStrip, BeforeAfterPreview, ServiceStandard, HowItWorks) render in view-source without JavaScript"
  artifacts: []
  key_links: []
---

<objective>
Run final performance and correctness verification for Phase 6 deliverables.

Purpose: Phase 6 has strict Lighthouse targets (90+ desktop, 75+ mobile) and correctness requirements (JSON-LD preservation, TypeScript budget, server/client split). This plan runs all verification checks and fixes any issues found.

Output: Verified performance scores and correctness confirmation. Any fixes applied to meet targets.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-brand-transformation/06-01-SUMMARY.md
@.planning/phases/06-brand-transformation/06-03-SUMMARY.md
@.planning/phases/06-brand-transformation/06-05-SUMMARY.md
@.planning/phases/06-brand-transformation/json-ld-baseline.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks (TypeScript, JSON-LD, SSR)</name>
  <files>none (verification + potential fixes)</files>
  <action>
Run comprehensive verification of Phase 6 deliverables:

**1. TypeScript error count:**
```bash
npx tsc --noEmit 2>&1 | grep "error TS" | wc -l
```
Must be <= 84. If higher, identify and fix the new errors immediately.

**2. JSON-LD verification:**
Start dev server, then:
```bash
curl -s http://localhost:3000 | grep -oP '<script type="application/ld\+json">.*?</script>' | wc -l
```
Compare output against `.planning/phases/06-brand-transformation/json-ld-baseline.txt`. Server-rendered schemas must match. Document any differences.

**3. Server rendering verification:**
```bash
curl -s http://localhost:3000 | grep -c "Business Summary"
```
Must return 1 (sr-only section is in the server HTML).

**4. Server component content verification:**
Verify that server components actually render content in the HTML response (not just empty divs waiting for hydration):
```bash
# Check for StatsStrip static content in HTML
curl -s http://localhost:3000 | grep -c "500+"
# Check for ServiceStandard content
curl -s http://localhost:3000 | grep -c "TotalGuard Standard"
```
At least one of these should return a match, proving server components deliver content without JS.

**5. Lighthouse attempt (best effort on Windows):**
Try running Lighthouse CLI:
```bash
npx lighthouse http://localhost:3000 --output=json --only-categories=performance --chrome-flags="--headless --no-sandbox" 2>/dev/null | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8');const r=JSON.parse(d);console.log('Performance:',Math.round(r.categories.performance.score*100))"
```

If Lighthouse CLI fails on Windows (common — Chrome headless issues, path problems), this is expected. Document the failure and note that the human checkpoint (Task 2) is the PRIMARY Lighthouse verification path.

**6. If automated Lighthouse works and performance < 90 (desktop) or < 75 (mobile):**
Common fixes to apply:
- Add `priority` prop to hero image if missing
- Add explicit `sizes` to all `next/image` components
- Check for unnecessary client-side JavaScript bundles
- Ensure no large unoptimized images
- Add `loading="lazy"` to below-fold images (next/image does this by default)

Document all scores and findings.
  </action>
  <verify>
1. TypeScript errors <= 84
2. JSON-LD scripts match baseline
3. Sr-only section in server HTML
4. Server component content verified in HTML response
5. Lighthouse scores documented (or CLI failure documented with manual steps)
  </verify>
  <done>All automated verification checks pass. Performance scores documented or deferred to human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 6: Homepage converted to Server Component with proper server/client split, cinematic hero treatment (parallax, gradient overlay, text reveals), before/after gallery with 20+ comparison sliders, JSON-LD preserved, performance verified.
  </what-built>
  <how-to-verify>
1. Open homepage in browser — all sections load and animate correctly
2. Verify cinematic hero: gradient overlay visible, text animates in with stagger, parallax effect on scroll
3. View Page Source — confirm server-rendered HTML (sr-only text, StatsStrip content, ServiceStandard content visible in source)
4. Open /gallery — before/after sliders work with drag interaction, 20+ entries visible
5. Run Lighthouse in Chrome DevTools (THIS IS THE PRIMARY LIGHTHOUSE CHECK):
   - Open DevTools > Lighthouse tab
   - Desktop mode: Performance should be 90+
   - Mobile mode: Performance should be 75+
   - If scores are below target, report the specific metrics (LCP, TBT, CLS) so we can fix
6. Check all pages still work: /services, /about, /contact, /gallery
7. Mobile test at 375px width — all sections responsive, hero looks good on mobile
  </how-to-verify>
  <resume-signal>Type "phase 6 approved" or describe remaining issues</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript error count <= 84
2. Lighthouse desktop >= 90, mobile >= 75 (verified by user in Chrome DevTools)
3. JSON-LD schemas match baseline
4. Server-rendered HTML confirmed (sr-only + server component content)
5. All pages functional (no regressions)
6. User has visually approved the result
</verification>

<success_criteria>
- All Phase 6 success criteria met
- Performance targets achieved (user-verified via Chrome DevTools Lighthouse)
- No regressions on other pages
- User approval received
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-transformation/06-06-SUMMARY.md`
</output>
