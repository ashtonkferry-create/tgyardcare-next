---
phase: 06-brand-transformation
plan: 05
type: execute
wave: 2
depends_on: ["04"]
files_modified:
  - src/app/gallery/GalleryContent.tsx
  - src/app/gallery/page.tsx
autonomous: false

must_haves:
  truths:
    - "Gallery page displays transformation pairs with draggable ComparisonSlider widgets"
    - "Gallery is filterable by service type using category pills"
    - "Gallery layout is 2 columns on desktop, 1 column on mobile"
    - "Each slider shows a neighborhood label for local SEO"
    - "Fullscreen viewing available on click"
    - "Gallery page metadata includes transformation-related keywords"
  artifacts:
    - path: "src/app/gallery/GalleryContent.tsx"
      provides: "Gallery page with comparison sliders, filtering, and fullscreen viewing"
      contains: "ComparisonSlider"
    - path: "src/app/gallery/page.tsx"
      provides: "Server component with metadata for gallery page"
  key_links:
    - from: "src/app/gallery/GalleryContent.tsx"
      to: "src/components/gallery/ComparisonSlider.tsx"
      via: "import and render for each TransformationPair"
      pattern: "import.*ComparisonSlider"
    - from: "src/app/gallery/GalleryContent.tsx"
      to: "src/lib/transformationData.ts"
      via: "import transformations array and serviceCategories"
      pattern: "import.*transformationData"
---

<objective>
Integrate comparison sliders into the existing gallery page with filtering by service type and fullscreen viewing.

Purpose: The existing gallery page at `/gallery` shows project photos in a grid. This plan extends it to include a "Before & After Transformations" section at the top featuring the ComparisonSlider components (built in Plan 04). Visitors can filter by service type, drag sliders to compare before/after, and click for fullscreen viewing. Neighborhood labels provide local SEO value.

Output: An enhanced gallery page with a dedicated before/after transformation section.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-brand-transformation/06-RESEARCH.md
@.planning/phases/06-brand-transformation/06-04-SUMMARY.md
@src/app/gallery/GalleryContent.tsx
@src/app/gallery/page.tsx
@src/components/gallery/ComparisonSlider.tsx
@src/lib/transformationData.ts
@src/components/ImageLightbox.tsx
@src/lib/galleryData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Before/After Transformations section to gallery page</name>
  <files>src/app/gallery/GalleryContent.tsx, src/app/gallery/page.tsx</files>
  <action>
Extend the existing GalleryContent.tsx to include a "Before & After Transformations" section above the existing project gallery grid.

**In GalleryContent.tsx:**

Add a new section at the top of the page content (after Navigation, before the existing gallery grid):

1. Import `ComparisonSlider` from `@/components/gallery/ComparisonSlider`
2. Import `transformations`, `serviceCategories` from `@/lib/transformationData`
3. Add state: `const [activeService, setActiveService] = useState<string>('all');`
4. Filter logic: `const filteredTransformations = activeService === 'all' ? transformations : transformations.filter(t => t.service === activeService);`

Section structure:
```
<section> "Before & After Transformations"
  <div> Category filter pills: "All" + each serviceCategory
    - Pills styled to match existing gallery filter pills (seasonal accent colors)
    - Active pill gets filled background, others are outlined
  </div>
  <div className="grid md:grid-cols-2 gap-6 md:gap-8">
    {filteredTransformations.map(pair => (
      <div key={pair.id} onClick={() => open fullscreen view with pair images}>
        <ComparisonSlider
          beforeSrc={pair.beforeSrc}
          afterSrc={pair.afterSrc}
          beforeAlt={pair.beforeAlt}
          afterAlt={pair.afterAlt}
          neighborhood={pair.neighborhood}
        />
        {pair.description && <p className="text-sm text-gray-400 mt-2">{pair.description}</p>}
      </div>
    ))}
  </div>
  {filteredTransformations.length === 0 && (
    <p>No transformations in this category yet. Check back soon!</p>
  )}
</section>
<SectionDivider or visual separator />
<existing gallery content continues below>
```

For fullscreen viewing on click:
- Check if the existing `ImageLightbox` component can accept the afterSrc image for fullscreen display
- If ImageLightbox works: use it with the `afterSrc` image (the result shot)
- If ImageLightbox doesn't support this flow cleanly: implement a simple fullscreen overlay that shows both before/after images side by side, with a close button
- Either approach satisfies the "fullscreen viewing available on click" requirement

**In page.tsx:**
- Keep existing metadata but ensure keywords include "before after" and "transformation"
- No structural changes needed (already a Server Component with GalleryContent as client island)

Match the existing seasonal theming patterns already in GalleryContent.tsx (it already uses `useSeasonalTheme` for accent colors and backgrounds).

CRITICAL: `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` must not increase above 84.
  </action>
  <verify>
1. Gallery page loads at `/gallery` with the new transformations section visible
2. Category filter pills work — clicking filters the displayed sliders
3. ComparisonSlider drag interaction works (test in browser)
4. Neighborhood labels visible on each slider
5. TypeScript error count at 84 (unchanged)
  </verify>
  <done>Gallery page has a before/after transformations section with comparison sliders, category filtering, and fullscreen viewing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Before/After transformation gallery with draggable comparison sliders, organized by service type with Madison neighborhood labels. Homepage converted to Server Component with client islands.
  </what-built>
  <how-to-verify>
1. Visit the homepage — verify all sections render correctly (hero, stats, services, reviews, etc.)
2. Right-click > View Page Source on homepage — verify the sr-only "Business Summary" section appears in the raw HTML (proves server rendering)
3. Visit /gallery — verify the "Before & After Transformations" section appears at the top
4. Drag a comparison slider left/right — verify smooth before/after reveal
5. Click category filter pills — verify filtering works
6. Check mobile view (375px) — verify sliders are 1-column and touch-draggable
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Gallery page has comparison sliders rendering
2. Category filtering works
3. Fullscreen viewing works on click
4. 2-col desktop / 1-col mobile layout
5. Neighborhood labels visible
6. TypeScript error count at 84
</verification>

<success_criteria>
- Comparison sliders integrated into gallery page
- Filtering by service type works
- Fullscreen viewing available
- Responsive layout (2-col desktop, 1-col mobile)
- User visually verifies the homepage and gallery
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-transformation/06-05-SUMMARY.md`
</output>
