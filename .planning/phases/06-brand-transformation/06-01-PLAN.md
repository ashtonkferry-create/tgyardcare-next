---
phase: 06-brand-transformation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-brand-transformation/json-ld-baseline.txt
  - src/components/home/HeroSection.tsx
  - src/components/home/StatsStrip.tsx
  - src/components/home/SeasonalServicesSection.tsx
  - src/components/home/ScrollRevealWrapper.tsx
  - src/components/WinterHero.tsx
  - src/components/SummerHero.tsx
  - src/components/FallHero.tsx
autonomous: true

must_haves:
  truths:
    - "All 7 homepage JSON-LD script tags are captured in a baseline file before any conversion begins"
    - "HeroSection.tsx renders the correct seasonal hero based on activeSeason context"
    - "StatsStrip.tsx is a Server Component (no 'use client') that renders 4 trust stats with CSS scroll-triggered animations"
    - "SeasonalServicesSection.tsx renders winter-specific or seasonal priority services based on season (client island — needs context)"
    - "WinterHero, SummerHero, and FallHero each feature: next/image with priority, parallax scroll effect, gradient overlay, staggered text reveal, and mobile srcSet"
  artifacts:
    - path: ".planning/phases/06-brand-transformation/json-ld-baseline.txt"
      provides: "Snapshot of all 7 JSON-LD scripts from the live/dev homepage for post-conversion comparison"
    - path: "src/components/home/HeroSection.tsx"
      provides: "Client island that switches between WinterHero, SummerHero, FallHero based on SeasonalThemeContext"
      contains: "use client"
    - path: "src/components/home/StatsStrip.tsx"
      provides: "SERVER COMPONENT with 4 trust stat counters — uses CSS animation-timeline or ScrollRevealWrapper for reveal"
      contains: "ScrollRevealWrapper"
    - path: "src/components/home/ScrollRevealWrapper.tsx"
      provides: "Tiny client island that wraps children with Framer Motion useInView reveal"
      contains: "use client"
    - path: "src/components/home/SeasonalServicesSection.tsx"
      provides: "Client island that renders WinterValueProposition + WinterPriorityServices or SeasonalPriorityServices"
      contains: "use client"
  key_links:
    - from: "src/components/home/HeroSection.tsx"
      to: "src/contexts/SeasonalThemeContext"
      via: "useSeasonalTheme() hook"
      pattern: "useSeasonalTheme"
    - from: "src/components/home/SeasonalServicesSection.tsx"
      to: "src/contexts/SeasonalThemeContext"
      via: "useSeasonalTheme() hook for season branching"
      pattern: "activeSeason"
    - from: "src/components/WinterHero.tsx"
      to: "next/image"
      via: "Optimized hero background with priority loading"
      pattern: "priority"
---

<objective>
Capture JSON-LD baseline, extract the first 3 inline sections from HomeContent.tsx into standalone components, and apply cinematic static hero treatment to all seasonal hero components.

Purpose: Before any server component conversion, we must snapshot the homepage JSON-LD output for post-conversion comparison. Then we extract the top 3 inline sections (hero switcher, stats strip, seasonal services) from the 546-line HomeContent.tsx monolith. StatsStrip becomes a Server Component (it has only static content). Finally, we apply the locked cinematic hero treatment (parallax, gradient overlay, staggered text reveals, optimized images) to WinterHero, SummerHero, and FallHero — the core visual transformation work.

Output: A JSON-LD baseline file, 3 new home section components (1 server + 2 client), a reusable ScrollRevealWrapper, and 3 upgraded hero components with cinematic treatment.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-brand-transformation/06-RESEARCH.md
@.planning/phases/06-brand-transformation/06-CONTEXT.md
@src/app/HomeContent.tsx
@src/app/page.tsx
@src/contexts/SeasonalThemeContext.tsx
@src/components/WinterHero.tsx
@src/components/SummerHero.tsx
@src/components/FallHero.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture JSON-LD baseline from live homepage</name>
  <files>.planning/phases/06-brand-transformation/json-ld-baseline.txt</files>
  <action>
Capture the current homepage JSON-LD output before any changes. Two approaches (try in order):

1. Use `curl -s https://tgyardcare.com | grep -oP '<script type="application/ld\+json">.*?</script>'` to extract all JSON-LD scripts from production.
2. If production is unreachable, start the dev server (`npm run dev`) and curl `http://localhost:3000`.
3. If neither works, read the source files to reconstruct: check `src/app/layout.tsx` for GlobalSchema + NavigationSchema + AutoSchema, and `src/app/HomeContent.tsx` for WebPageSchema. Document which schemas render on the homepage route.

Save the full output (all 7 expected JSON-LD script tags) to `.planning/phases/06-brand-transformation/json-ld-baseline.txt`.

Expected schemas on homepage:
- Organization (from GlobalSchema)
- LocalBusiness (from GlobalSchema)
- WebSite (from GlobalSchema)
- SiteNavigationElement (from NavigationSchema)
- WebPage (from AutoSchema — client-rendered)
- BreadcrumbList (from AutoSchema — client-rendered)
- WebPage (from WebPageSchema in HomeContent)

Count the scripts and note the total at the top of the file.
  </action>
  <verify>File exists at `.planning/phases/06-brand-transformation/json-ld-baseline.txt` and contains JSON-LD content. Count matches 7 (or documents why fewer were captured, e.g., client-rendered ones not in curl output).</verify>
  <done>Baseline file exists with documented JSON-LD output from the homepage.</done>
</task>

<task type="auto">
  <name>Task 2: Extract HeroSection, StatsStrip (server), SeasonalServicesSection, and ScrollRevealWrapper</name>
  <files>src/components/home/HeroSection.tsx, src/components/home/StatsStrip.tsx, src/components/home/SeasonalServicesSection.tsx, src/components/home/ScrollRevealWrapper.tsx</files>
  <action>
Create `src/components/home/` directory and extract 3 inline sections from HomeContent.tsx into standalone files. Also create a tiny client wrapper for scroll animations. Do NOT modify HomeContent.tsx yet — just create the new files.

**ScrollRevealWrapper.tsx** — Tiny client island for scroll reveal:
```
'use client';
import { useRef, type ReactNode } from 'react';
import { motion, useInView } from 'framer-motion';

interface ScrollRevealWrapperProps {
  children: ReactNode;
  className?: string;
  delay?: number;
}

export function ScrollRevealWrapper({ children, className, delay = 0 }: ScrollRevealWrapperProps) {
  const ref = useRef<HTMLDivElement>(null);
  const isInView = useInView(ref, { once: true, margin: '-50px' });
  return (
    <motion.div
      ref={ref}
      className={className}
      initial={{ opacity: 0, y: 20 }}
      animate={isInView ? { opacity: 1, y: 0 } : { opacity: 0, y: 20 }}
      transition={{ duration: 0.5, delay }}
    >
      {children}
    </motion.div>
  );
}
```
This is the ONLY component in this plan that uses Framer Motion. It wraps static server content with a reveal animation.

**HeroSection.tsx** — Client island (needs SeasonalThemeContext):
```
'use client';
import { useSeasonalTheme } from '@/contexts/SeasonalThemeContext';
import { WinterHero } from '@/components/WinterHero';
import { SummerHero } from '@/components/SummerHero';
import { FallHero } from '@/components/FallHero';

export function HeroSection() {
  const { activeSeason } = useSeasonalTheme();
  if (activeSeason === 'winter') return <WinterHero />;
  if (activeSeason === 'fall') return <FallHero />;
  return <SummerHero />;
}
```

**StatsStrip.tsx** — SERVER COMPONENT (NO 'use client'):
- Do NOT add `'use client'` directive — this is a Server Component
- Do NOT import Framer Motion (motion.div) directly — it requires 'use client'
- Do NOT import `useScrollReveal` — it's a client hook
- Move the `trustStats` array into this component
- The component renders the `<section className="py-6 md:py-8 bg-gradient-to-r ...">` block
- Wrap the stats grid in `<ScrollRevealWrapper>` for the scroll-triggered reveal animation
- The stat numbers are static (500+, 4.9, 12, 24hr) — no state needed
- Import lucide-react icons as needed (they work in server components)
- Strictly type the component: `export function StatsStrip(): React.JSX.Element`

**SeasonalServicesSection.tsx** — Client island (needs SeasonalThemeContext):
- Must include `'use client'` directive
- Import and use `useSeasonalTheme` for `activeSeason`
- Import WinterValueProposition, WinterPriorityServices, SeasonalPriorityServices
- Renders the winter vs non-winter conditional block

CRITICAL: Run `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` before AND after creating these files. The count must not increase above 84. All new files must be strictly typed — no `any`, no missing types.
  </action>
  <verify>
1. All 4 files exist in `src/components/home/`
2. `grep "'use client'" src/components/home/StatsStrip.tsx` returns NOTHING (it is a Server Component)
3. `grep "'use client'" src/components/home/HeroSection.tsx` returns a match
4. `grep "'use client'" src/components/home/SeasonalServicesSection.tsx` returns a match
5. `grep "'use client'" src/components/home/ScrollRevealWrapper.tsx` returns a match
6. `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` shows no increase from baseline (84)
7. No `any` type appears in any new file
  </verify>
  <done>Four new component files exist: StatsStrip as Server Component, HeroSection + SeasonalServicesSection + ScrollRevealWrapper as client islands. Zero new TS errors.</done>
</task>

<task type="auto">
  <name>Task 3: Apply cinematic static hero treatment to WinterHero, SummerHero, and FallHero</name>
  <files>src/components/WinterHero.tsx, src/components/SummerHero.tsx, src/components/FallHero.tsx</files>
  <action>
Open each of the 3 seasonal hero components and apply the locked cinematic static hero treatment. These are the EXISTING hero components — they already render content. This task upgrades their visual impact.

For EACH of WinterHero.tsx, SummerHero.tsx, FallHero.tsx, apply:

1. **next/image with priority loading**: Replace any `<img>` or CSS background-image with `<Image>` from `next/image`. Use `priority` prop (no lazy loading for above-fold hero). Use `fill` + `object-cover` for full-bleed background effect. Add `sizes="100vw"` for responsive loading. Add `quality={85}` for balance.

2. **Mobile srcSet via next/image**: next/image handles srcSet automatically when using responsive sizes. Ensure `sizes` prop is set: `sizes="100vw"` so the browser picks appropriate resolution for mobile vs desktop.

3. **Parallax scroll effect**: Add a CSS-based parallax using `will-change: transform` and a subtle `translateY` on scroll. Use a simple CSS approach:
   - Container: `overflow: hidden` (clips the parallax image)
   - Image wrapper: `height: 120%`, `top: -10%`, `position: absolute`
   - Add `style={{ transform: 'translateZ(-1px) scale(1.5)' }}` OR use `background-attachment: fixed` as a simpler CSS-only parallax
   - Since these are 'use client' components (they're inside HeroSection which is client), you CAN use a small `useEffect` + scroll listener for smoother parallax: `translateY(scrollY * 0.3)`

4. **Gradient overlay**: Add a dark gradient overlay on top of the hero image for text readability:
   ```
   <div className="absolute inset-0 bg-gradient-to-b from-black/60 via-black/30 to-black/70 z-[1]" />
   ```
   Adjust gradient stops per season for visual variety.

5. **Staggered text reveal animation**: The hero headline, subheadline, and CTA should animate in with staggered delays using Framer Motion (these components are already client-side via HeroSection wrapper):
   ```
   <motion.h1 initial={{ opacity: 0, y: 30 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.8, delay: 0.2 }}>
   <motion.p initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: 0.5 }}>
   <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6, delay: 0.8 }}>
   ```

6. **Structure for future video swap**: Ensure the image is in a clearly labeled slot that can be swapped for a `<video>` element later:
   ```
   {/* Hero Media — swap Image for <video> when footage is available */}
   <div className="absolute inset-0">
     <Image src={heroImage} alt="..." fill priority ... />
   </div>
   ```

IMPORTANT: These hero components already have `'use client'` (or are rendered inside a client boundary). Framer Motion is fine here.

CRITICAL: Run `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` after changes. Must not increase above 84.
  </action>
  <verify>
1. Each hero component uses `next/image` with `priority` prop
2. Each hero has a gradient overlay div
3. Each hero has staggered motion animation on text elements
4. Each hero has parallax-ready image container with overflow:hidden
5. Each hero has a comment indicating the video swap slot
6. `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` shows no increase from baseline (84)
  </verify>
  <done>All 3 seasonal hero components feature cinematic treatment: optimized next/image with priority, parallax scroll, gradient overlay, staggered text reveals, mobile-responsive srcSet, and future video swap structure.</done>
</task>

</tasks>

<verification>
1. JSON-LD baseline file exists and documents 7 schema scripts
2. Four new component files created in `src/components/home/`
3. StatsStrip is a Server Component (no 'use client')
4. HeroSection, SeasonalServicesSection, ScrollRevealWrapper are client islands
5. All 3 hero components upgraded with cinematic treatment
6. TypeScript error count remains at 84 (not increased)
7. Existing HomeContent.tsx is untouched (still works as-is)
</verification>

<success_criteria>
- Baseline captured before any modifications
- Components extracted with correct server/client split per CONTEXT.md
- Hero components have parallax, gradient overlay, text reveals, optimized images
- Zero new TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-transformation/06-01-SUMMARY.md`
</output>
