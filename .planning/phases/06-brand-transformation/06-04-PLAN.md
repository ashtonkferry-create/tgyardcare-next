---
phase: 06-brand-transformation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/gallery/ComparisonSlider.tsx
  - src/lib/transformationData.ts
autonomous: true

must_haves:
  truths:
    - "ComparisonSlider renders two images with a draggable divider that reveals before/after"
    - "Dragging the handle left/right clips the 'before' image to reveal the 'after' image underneath"
    - "Slider works with pointer events (mouse + touch) and captures pointer for smooth drag"
    - "TransformationPair type is exported and strictly typed with service categories and neighborhood strings"
    - "Transformation data array contains 20+ placeholder entries organized by service type across 8 categories"
  artifacts:
    - path: "src/components/gallery/ComparisonSlider.tsx"
      provides: "Custom before/after comparison slider using Pointer Events API"
      contains: "use client"
      min_lines: 60
    - path: "src/lib/transformationData.ts"
      provides: "Static transformation data with 20+ typed pairs and 8 service categories"
      exports: ["TransformationPair", "transformations", "serviceCategories"]
  key_links:
    - from: "src/components/gallery/ComparisonSlider.tsx"
      to: "next/image"
      via: "Image component for optimized before/after images"
      pattern: "import Image from"
---

<objective>
Build the custom before/after comparison slider component and the transformation data model for the gallery page.

Purpose: The gallery needs a draggable image comparison widget to showcase before/after lawn transformations. Rather than depending on `react-compare-slider` (stale, unverified React 19 compat), we build a lightweight custom slider (~80 lines) using the Pointer Events API. The transformation data model defines the static JSON structure for all 20+ before/after pairs (HERO-04 requirement).

Output: A reusable ComparisonSlider component and a typed transformation data file with 20+ entries ready for the gallery page (Plan 05).
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-brand-transformation/06-RESEARCH.md
@src/app/gallery/GalleryContent.tsx
@src/lib/galleryData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build custom ComparisonSlider component</name>
  <files>src/components/gallery/ComparisonSlider.tsx</files>
  <action>
Create `src/components/gallery/ComparisonSlider.tsx` — a custom before/after image comparison slider.

Requirements:
- `'use client'` directive
- Uses Pointer Events API (pointerdown, pointermove, pointerup) for cross-device drag support
- Uses `setPointerCapture` on drag start for smooth tracking even when cursor leaves the element
- clipPath-based reveal: After image is full-width underneath, Before image is clipped from the right
- Slider handle: vertical white line with a circular drag handle centered on it (left/right arrow icon)
- "Before" label pinned bottom-left, "After" label pinned bottom-right (both with dark backdrop)
- Optional `neighborhood` prop for a location label (top-left, white pill)
- Aspect ratio 4:3 via `aspect-[4/3]` class
- Uses `next/image` with `fill` and `object-cover` for both images
- `sizes` prop: `"(max-width: 768px) 100vw, 50vw"` (matches 2-col desktop / 1-col mobile layout)
- `touch-none` and `select-none` on container to prevent scroll interference during drag
- Initial position: 50%

Interface:
```typescript
interface ComparisonSliderProps {
  beforeSrc: string;
  afterSrc: string;
  beforeAlt: string;
  afterAlt: string;
  neighborhood?: string;
}
```

Follow the code example from 06-RESEARCH.md but ensure:
- No `any` types
- All event handlers properly typed as `React.PointerEvent<HTMLDivElement>`
- Component export: `export function ComparisonSlider({ ... }: ComparisonSliderProps)`

CRITICAL: `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` must not increase above 84.
  </action>
  <verify>
1. File exists at `src/components/gallery/ComparisonSlider.tsx`
2. Has `'use client'` directive
3. TypeScript compiles with no new errors
4. No `any` types in the file
  </verify>
  <done>ComparisonSlider component exists, is strictly typed, and compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create transformation data model with 20+ placeholder entries</name>
  <files>src/lib/transformationData.ts</files>
  <action>
Create `src/lib/transformationData.ts` with the data model for before/after photo pairs.

Define and export:

```typescript
export type ServiceCategory = 'mowing' | 'leaf-cleanup' | 'snow-removal' | 'hardscaping' | 'gutter-cleaning' | 'mulching' | 'spring-cleanup' | 'fall-cleanup';

export interface TransformationPair {
  id: string;
  beforeSrc: string;
  afterSrc: string;
  beforeAlt: string;
  afterAlt: string;
  service: ServiceCategory;
  neighborhood: string;
  description?: string;
}

export const serviceCategories: { value: ServiceCategory; label: string }[] = [
  { value: 'mowing', label: 'Lawn Mowing' },
  { value: 'leaf-cleanup', label: 'Leaf Cleanup' },
  { value: 'snow-removal', label: 'Snow Removal' },
  { value: 'hardscaping', label: 'Hardscaping' },
  { value: 'gutter-cleaning', label: 'Gutter Cleaning' },
  { value: 'mulching', label: 'Mulching' },
  { value: 'spring-cleanup', label: 'Spring Cleanup' },
  { value: 'fall-cleanup', label: 'Fall Cleanup' },
];
```

IMPORTANT: Seed 20+ placeholder transformation pairs (HERO-04 requirement). Check what images exist in `public/lovable-uploads/` first. For placeholders, reuse existing images (same image for before and after is fine — the slider will still be structurally complete).

Distribution across all 8 service categories (at minimum 2 per category, more for popular services):
- mowing: 4 entries (Middleton, Waunakee, West Side Madison, Sun Prairie)
- leaf-cleanup: 3 entries (Fitchburg, Verona, Cross Plains)
- snow-removal: 3 entries (Downtown Madison, Middleton, Waunakee)
- hardscaping: 2 entries (Sun Prairie, Mount Horeb)
- gutter-cleaning: 2 entries (West Side Madison, Fitchburg)
- mulching: 2 entries (Verona, Windsor)
- spring-cleanup: 2 entries (Middleton, Waunakee)
- fall-cleanup: 2 entries (Sun Prairie, Cross Plains)

Total: 20 entries minimum.

Use real Madison-area neighborhood names for local SEO value. Each entry should have unique, descriptive alt text mentioning the neighborhood and service.

Example placeholder entry:
```typescript
{
  id: 'mowing-middleton-01',
  beforeSrc: '/lovable-uploads/mowing-ba-1.png',
  afterSrc: '/lovable-uploads/mowing-ba-1.png',
  beforeAlt: 'Overgrown lawn before mowing service in Middleton',
  afterAlt: 'Freshly mowed lawn after TotalGuard service in Middleton',
  service: 'mowing',
  neighborhood: 'Middleton',
  description: 'Weekly mowing transformation — 3 weeks of consistent service',
}
```

This is a NO `'use client'` file — it's pure data, importable from both server and client components.

CRITICAL: `npx tsc --noEmit 2>&1 | grep "error TS" | wc -l` must not increase above 84.
  </action>
  <verify>
1. File exists at `src/lib/transformationData.ts`
2. Exports `TransformationPair`, `ServiceCategory`, `serviceCategories`, and `transformations`
3. `transformations` array has 20+ entries: `grep -c "id:" src/lib/transformationData.ts` returns 20 or more
4. All 8 service categories are represented: `grep -oP "service: '([^']+)'" src/lib/transformationData.ts | sort -u | wc -l` returns 8
5. No `'use client'` in the file
6. TypeScript compiles with no new errors
  </verify>
  <done>Transformation data model exists with typed interfaces, 8 service categories, and 20+ placeholder entries covering all categories with real Madison neighborhood names.</done>
</task>

</tasks>

<verification>
1. ComparisonSlider.tsx exists and compiles
2. transformationData.ts exists with 20+ typed entries across 8 categories
3. TypeScript error count remains at 84
4. No external dependencies added (zero new packages)
</verification>

<success_criteria>
- Custom slider built with Pointer Events API (no library dependency)
- Data model strictly typed with 8 service categories
- 20+ placeholder entries with real neighborhood names for local SEO
- Zero new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-transformation/06-04-SUMMARY.md`
</output>
