---
phase: 16-content-seo-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - automation/n8n-workflows/TG-96-gsc-daily-sync.json
  - automation/n8n-workflows/TG-45-keyword-rank-tracker.json
  - automation/n8n-workflows/TG-97-rank-drop-detector.json
autonomous: true

must_haves:
  truths:
    - "GSC performance data syncs to Supabase daily"
    - "Keyword rankings for target terms are tracked daily"
    - "Rank drops > 3 positions trigger immediate Telegram alert"
  artifacts:
    - path: "automation/n8n-workflows/TG-96-gsc-daily-sync.json"
      provides: "GSC daily performance sync workflow"
      contains: "TG-96"
    - path: "automation/n8n-workflows/TG-45-keyword-rank-tracker.json"
      provides: "Keyword rank tracking workflow"
      contains: "TG-45"
    - path: "automation/n8n-workflows/TG-97-rank-drop-detector.json"
      provides: "Rank drop detection and alerting workflow"
      contains: "TG-97"
  key_links:
    - from: "TG-96"
      to: "Supabase gsc_performance / seo_rankings"
      via: "HTTP POST to upsert daily metrics"
      pattern: "gsc_performance|seo_rankings"
    - from: "TG-45"
      to: "Supabase seo_rankings"
      via: "HTTP POST to store daily rank snapshots"
      pattern: "seo_rankings"
    - from: "TG-97"
      to: "Telegram"
      via: "Send rank drop alert when delta > 3"
      pattern: "rank.*drop|position.*change"
---

<objective>
Deploy the SEO monitoring pipeline: TG-96 GSC daily sync, TG-45 keyword rank tracker, and TG-97 rank drop detector with Telegram alerts.

Purpose: Track search performance daily and alert on significant rank drops so Vance can react before traffic loss compounds. Covers SEO-04, SEO-05, SEO-06.
Output: Three n8n workflows deployed and active, syncing GSC data daily, tracking keyword rankings, and alerting on drops > 3 positions.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@automation/n8n-workflows/TG-96-gsc-daily-sync.json
@automation/n8n-workflows/TG-45-keyword-rank-tracker.json
@automation/n8n-workflows/TG-97-rank-drop-detector.json

Key facts:
- Google Search Console service account exists for workely.ai but TotalGuard GSC access needs verification
- TotalGuard site: tgyardcare.com
- GSC API scope: https://www.googleapis.com/auth/webmasters
- Supabase URL: https://lwtmvzhwekgdxkaisfra.supabase.co
- Supabase anon key: sb_publishable_DeX21ldoDKl3NyJeFZzR1w_eWBUkx-v
- Telegram credential ID: V404qLIDXjmyzNeS
- n8n API base: https://tgyardcare.app.n8n.cloud
- n8n API key provided in phase context
- n8n API PUT sometimes returns 400 — deploy via UI import if API fails
- Existing Supabase tables may include: gsc_performance, seo_rankings, seo_keywords (check workflow JSONs)
- 15 workflows currently active (may be 18 after Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update TG-96, TG-45, TG-97 workflow JSONs</name>
  <files>
    automation/n8n-workflows/TG-96-gsc-daily-sync.json
    automation/n8n-workflows/TG-45-keyword-rank-tracker.json
    automation/n8n-workflows/TG-97-rank-drop-detector.json
  </files>
  <action>
    Read all three workflow JSONs fully. For each workflow:

    1. **TG-96 (GSC daily sync)**:
       - Verify it queries GSC Search Analytics API for tgyardcare.com (not workely.ai)
       - Schedule should be daily (e.g., `0 6 * * *` for 6am UTC / 1am CT — runs before TG-45)
       - Must pull: clicks, impressions, CTR, position by page and query
       - Date range: last 3 days (GSC data has 2-day lag)
       - Upserts results to Supabase table (gsc_performance or similar)
       - Fix any Supabase URL issues (no duplicate https://, no placeholder IDs)
       - Replace Supabase project ID placeholder with `lwtmvzhwekgdxkaisfra`
       - Add automation_runs logging
       - GSC authentication: check what method the workflow uses (OAuth2, service account JWT, or API key). If it uses a credential reference, note the credential name needed for n8n setup. If it hardcodes a key, convert to credential reference.

    2. **TG-45 (keyword rank tracker)**:
       - Schedule should be daily, AFTER TG-96 (e.g., `0 8 * * *` for 8am UTC / 3am CT)
       - Must track target keywords relevant to TotalGuard services:
         - "lawn care madison wi", "lawn mowing madison", "fertilization service madison wi"
         - "yard care near me", "landscaping madison wi", "snow removal madison wi"
         - Plus city variants for all 12 service areas
       - Pulls position data from GSC (or from gsc_performance table populated by TG-96)
       - Stores daily rank snapshots in Supabase (seo_rankings or keyword_rankings table)
       - Fix Supabase URL/key issues same as above
       - Add automation_runs logging

    3. **TG-97 (rank drop detector)**:
       - Schedule should be daily, AFTER TG-45 (e.g., `0 10 * * *` for 10am UTC / 5am CT)
       - Reads rank snapshots from last 2 days
       - Compares: if any keyword dropped > 3 positions, trigger alert
       - Telegram alert format: "RANK DROP: [keyword] dropped from position [X] to [Y] (-[N] positions)"
       - Use Telegram credential V404qLIDXjmyzNeS
       - Also alert if a previously-ranking keyword disappeared entirely
       - Fix Supabase URL/key issues
       - Add automation_runs logging

    Save all updated JSONs locally.
  </action>
  <verify>
    - `grep -c "SUPABASE_PROJECT_ID" automation/n8n-workflows/TG-96-gsc-daily-sync.json` returns 0
    - `grep -c "https://https://" automation/n8n-workflows/TG-96-gsc-daily-sync.json` returns 0
    - Same checks for TG-45 and TG-97
    - `grep "tgyardcare.com" automation/n8n-workflows/TG-96-gsc-daily-sync.json` confirms correct site
    - `grep "V404qLIDXjmyzNeS\|telegram" automation/n8n-workflows/TG-97-rank-drop-detector.json` confirms Telegram integration
    - All three JSONs are valid JSON (parse without errors)
  </verify>
  <done>All three monitoring workflow JSONs are clean, correctly configured for tgyardcare.com, with proper scheduling sequence (TG-96 6am -> TG-45 8am -> TG-97 10am UTC), and TG-97 has Telegram alerting on drops > 3 positions.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy TG-96, TG-45, TG-97 to n8n and activate</name>
  <files>
    automation/n8n-workflows/TG-96-gsc-daily-sync.json
    automation/n8n-workflows/TG-45-keyword-rank-tracker.json
    automation/n8n-workflows/TG-97-rank-drop-detector.json
  </files>
  <action>
    Deploy all three workflows to n8n using the API.

    For each workflow:
    1. Check if workflow already exists on n8n by listing workflows and matching by name
    2. If exists: PUT to update, if not: POST to create
    3. Activate via POST /api/v1/workflows/{id}/activate

    **Activation order (matches schedule dependency):**
    1. TG-96 (GSC sync) — runs first daily, populates data
    2. TG-45 (rank tracker) — runs second, reads GSC data
    3. TG-97 (rank drop detector) — runs third, compares snapshots

    **GSC credential note:** TG-96 needs GSC API access. Check if the workflow references an n8n credential for Google. If so, note in summary that Vance needs to:
    1. Add a Google OAuth2 or Service Account credential in n8n
    2. Grant the service account access to tgyardcare.com in GSC
    3. Name the credential to match what TG-96 references

    If n8n API PUT fails with 400, note for UI import.

    After deployment, verify by listing active workflows.
  </action>
  <verify>
    - n8n API GET /api/v1/workflows returns TG-96, TG-45, TG-97 in active state
    - Workflow count increased appropriately
  </verify>
  <done>TG-96 active (daily GSC sync), TG-45 active (daily rank tracking), TG-97 active (rank drop alerting via Telegram). GSC credential setup documented if needed.</done>
</task>

</tasks>

<verification>
- TG-96 scheduled daily before TG-45
- TG-45 scheduled daily before TG-97
- TG-97 sends Telegram alerts on rank drops > 3 positions
- All workflows use clean Supabase URLs
- GSC queries target tgyardcare.com (not workely.ai)
</verification>

<success_criteria>
- SEO-04: TG-96 active, syncing GSC data daily to Supabase
- SEO-05: TG-45 active, tracking keyword rankings for target terms across 12 cities
- SEO-06: TG-97 active, alerting via Telegram when any keyword drops > 3 positions
</success_criteria>

<output>
After completion, create `.planning/phases/16-content-seo-engine/16-02-SUMMARY.md`
</output>
