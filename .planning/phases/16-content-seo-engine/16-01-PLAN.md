---
phase: 16-content-seo-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - automation/n8n-workflows/TG-99-blog-auto-publisher.json
  - automation/n8n-workflows/TG-103-city-content-generator.json
  - automation/n8n-workflows/TG-47-indexnow-submitter.json
autonomous: true

must_haves:
  truths:
    - "TG-99 publishes a new blog post 3x/week (Mon/Wed/Fri 10am CT)"
    - "TG-103 generates city-specific content pages on demand"
    - "Every new page triggers IndexNow submission via TG-47"
    - "New blog posts include internal links to relevant service and city pages"
  artifacts:
    - path: "automation/n8n-workflows/TG-99-blog-auto-publisher.json"
      provides: "Blog auto-publisher workflow"
      contains: "TG-99"
    - path: "automation/n8n-workflows/TG-103-city-content-generator.json"
      provides: "City content generator workflow"
      contains: "TG-103"
    - path: "automation/n8n-workflows/TG-47-indexnow-submitter.json"
      provides: "IndexNow submission workflow"
      contains: "TG-47"
  key_links:
    - from: "TG-99"
      to: "TG-47"
      via: "Execute Workflow node after blog post published"
      pattern: "TG-47|indexnow"
    - from: "TG-103"
      to: "TG-47"
      via: "Execute Workflow node after city page published"
      pattern: "TG-47|indexnow"
    - from: "TG-99"
      to: "Supabase blog_posts"
      via: "HTTP POST to insert blog post"
      pattern: "blog_posts"
---

<objective>
Deploy the content production pipeline: TG-99 blog auto-publisher, TG-103 city content generator, TG-47 IndexNow submitter, and internal linking logic.

Purpose: Automate content creation (3 blog posts/week + city pages) with instant search engine notification and cross-linking to service/city pages. Covers SEO-01, SEO-02, SEO-03, SEO-08.
Output: Three n8n workflows deployed and active, producing content on schedule with IndexNow pings and internal links.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@automation/n8n-workflows/TG-99-blog-auto-publisher.json
@automation/n8n-workflows/TG-103-city-content-generator.json
@automation/n8n-workflows/TG-47-indexnow-submitter.json

Key facts from memory:
- TG-99 already deployed on n8n (workflow ID: ANcn1PWAky4GoCbb) but was deactivated during clean slate
- Schedule: Mon/Wed/Fri 3pm UTC (10am CT) — cron `0 15 * * 1,3,5`
- 70+ topics across 5 categories with 4-season rotation
- Claude Sonnet generates 1200-1500 word articles
- Quality gates: word count >= 800, title <= 65 chars, meta <= 155 chars
- Pings IndexNow, logs to automation_runs, SMS notification
- IndexNow key file exists: public/tg2026indexnow8x4k.txt
- Supabase URL: https://lwtmvzhwekgdxkaisfra.supabase.co
- Supabase anon key: sb_publishable_DeX21ldoDKl3NyJeFZzR1w_eWBUkx-v
- Telegram credential ID: V404qLIDXjmyzNeS
- n8n API base: https://tgyardcare.app.n8n.cloud
- n8n API key provided in phase context
- n8n API PUT returns 400 for some operations — deploy via UI import if API fails
- TG-94 has duplicate URL bug — avoid using TG-94 Supabase pattern; use clean URLs
- 15 workflows currently active on n8n
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update TG-99, TG-103, TG-47 workflow JSONs</name>
  <files>
    automation/n8n-workflows/TG-99-blog-auto-publisher.json
    automation/n8n-workflows/TG-103-city-content-generator.json
    automation/n8n-workflows/TG-47-indexnow-submitter.json
  </files>
  <action>
    Read all three workflow JSONs fully. For each workflow:

    1. **TG-99 (blog auto-publisher)**:
       - Verify Supabase URLs use clean format (no duplicate https://) — fix any `==https://` or `https://https://` patterns
       - Replace any placeholder `SUPABASE_PROJECT_ID` with `lwtmvzhwekgdxkaisfra`
       - Replace placeholder Supabase keys with credential references (use n8n credential binding, not hardcoded keys)
       - Verify schedule is `0 15 * * 1,3,5` (Mon/Wed/Fri 3pm UTC = 10am CT)
       - Verify IndexNow ping node exists and calls TG-47 or directly pings IndexNow API
       - Add internal linking logic: after generating blog content, query Supabase for matching service pages (by keyword overlap) and city pages (by location mention), then inject 2-3 internal links into the blog body before saving
       - Verify automation_runs logging node exists
       - Verify Telegram notification node uses credential ID V404qLIDXjmyzNeS (not OWNER_TELEGRAM_CHAT_ID placeholder)
       - Ensure Claude API node uses Sonnet model for content generation

    2. **TG-103 (city content generator)**:
       - Audit for same Supabase URL/key issues as TG-99
       - Verify it generates unique content for each city (not templated copy-paste)
       - Ensure it calls TG-47 IndexNow after publishing each city page
       - Add internal linking: link to parent service page and 2-3 related blog posts

    3. **TG-47 (IndexNow submitter)**:
       - Verify IndexNow API endpoint: `https://api.indexnow.org/indexnow`
       - Verify key matches: `tg2026indexnow8x4k` (from public/tg2026indexnow8x4k.txt)
       - Verify host: `tgyardcare.com`
       - Ensure it accepts a URL parameter (or array of URLs) from calling workflows
       - Add error handling: log failures to automation_runs with error details

    Save all updated JSONs locally.
  </action>
  <verify>
    - `grep -c "SUPABASE_PROJECT_ID" automation/n8n-workflows/TG-99-blog-auto-publisher.json` returns 0
    - `grep -c "https://https://" automation/n8n-workflows/TG-99-blog-auto-publisher.json` returns 0
    - `grep -c "==https://" automation/n8n-workflows/TG-99-blog-auto-publisher.json` returns 0
    - `grep "indexnow\|IndexNow\|TG-47" automation/n8n-workflows/TG-99-blog-auto-publisher.json` shows IndexNow integration
    - `grep "internal.link\|service_pages\|city_pages" automation/n8n-workflows/TG-99-blog-auto-publisher.json` shows internal linking logic
    - Same checks for TG-103 and TG-47
  </verify>
  <done>All three workflow JSONs are clean (no placeholder IDs, no duplicate URLs), include IndexNow integration, and contain internal linking logic.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy TG-99, TG-103, TG-47 to n8n and activate</name>
  <files>
    automation/n8n-workflows/TG-99-blog-auto-publisher.json
    automation/n8n-workflows/TG-103-city-content-generator.json
    automation/n8n-workflows/TG-47-indexnow-submitter.json
  </files>
  <action>
    Deploy all three workflows to n8n using the API. The n8n instance is at https://tgyardcare.app.n8n.cloud with the API key from phase context.

    For each workflow:
    1. Check if workflow already exists on n8n by listing workflows and matching by name
    2. If exists: PUT to update the workflow with the cleaned JSON
    3. If not exists: POST to create the workflow
    4. Activate the workflow via POST /api/v1/workflows/{id}/activate

    **Important caveats:**
    - n8n API PUT sometimes returns 400 "Could not find property option" — if this happens, note it in the summary and instruct to import via n8n UI instead
    - TG-99 already has n8n ID ANcn1PWAky4GoCbb — try updating that first
    - TG-47 and TG-103 may or may not exist on n8n already — check first
    - After activation, verify each workflow appears in the active list

    Activation order matters:
    1. First activate TG-47 (IndexNow) — it's called by the others
    2. Then activate TG-103 (city content) — lower frequency
    3. Then activate TG-99 (blog publisher) — this is the main cron

    After deployment, verify by listing active workflows and confirming all three appear.
  </action>
  <verify>
    - n8n API GET /api/v1/workflows returns TG-99, TG-103, TG-47 in active state
    - `curl -s -H "X-N8N-API-KEY: $KEY" https://tgyardcare.app.n8n.cloud/api/v1/workflows | grep -c '"active":true'` shows count increased by 3 (from 15 to 18)
  </verify>
  <done>TG-99 active and scheduled Mon/Wed/Fri, TG-103 active and ready for triggers, TG-47 active and accepting IndexNow requests. Total active workflows: 18.</done>
</task>

</tasks>

<verification>
- TG-99 is active on n8n with correct schedule (Mon/Wed/Fri 3pm UTC)
- TG-47 is active and responds to Execute Workflow calls
- TG-103 is active and ready to generate city content
- All workflows use clean Supabase URLs (no duplicates)
- Internal linking logic exists in TG-99 and TG-103
- IndexNow integration wired in both content-producing workflows
</verification>

<success_criteria>
- SEO-01: TG-99 active, scheduled 3x/week, generating 1200-1500 word posts with quality gates
- SEO-02: TG-103 active, generating unique city-specific content on demand
- SEO-03: TG-47 active, receiving IndexNow pings from both TG-99 and TG-103
- SEO-08: Internal linking logic injecting 2-3 links per new post to service/city pages
</success_criteria>

<output>
After completion, create `.planning/phases/16-content-seo-engine/16-01-SUMMARY.md`
</output>
