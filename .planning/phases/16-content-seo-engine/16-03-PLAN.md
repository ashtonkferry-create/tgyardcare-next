---
phase: 16-content-seo-engine
plan: 03
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - automation/n8n-workflows/TG-50-content-refresher.json
autonomous: true

must_haves:
  truths:
    - "Blog posts older than 90 days are auto-detected as stale"
    - "Stale posts are queued for AI-powered content refresh"
    - "Refreshed posts get updated published_at and re-submitted to IndexNow"
    - "Stale content detection runs weekly with a Telegram summary"
  artifacts:
    - path: "automation/n8n-workflows/TG-50-content-refresher.json"
      provides: "Content staleness detection and refresh workflow"
      contains: "TG-50"
  key_links:
    - from: "TG-50"
      to: "Supabase blog_posts"
      via: "Query for posts with published_at > 90 days ago"
      pattern: "blog_posts.*published_at|90.*day"
    - from: "TG-50"
      to: "TG-47 IndexNow"
      via: "Re-submit refreshed post URLs after update"
      pattern: "TG-47|indexnow"
    - from: "TG-50"
      to: "Telegram"
      via: "Weekly summary of stale posts detected and refreshed"
      pattern: "telegram|stale.*summary"
---

<objective>
Deploy TG-50 content refresher: detects stale blog posts (> 90 days), AI-refreshes them, and re-submits to IndexNow.

Purpose: Keep content fresh for SEO value retention. Google favors recently-updated content, and 90-day-old posts start losing ranking power. Covers SEO-07.
Output: TG-50 deployed and active, running weekly to detect and refresh stale content.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@automation/n8n-workflows/TG-50-content-refresher.json
@.planning/phases/16-content-seo-engine/16-02-SUMMARY.md

Key facts:
- TG-50 workflow JSON exists locally
- Depends on Plan 02 because refresh priority should factor in rank data (refresh declining posts first)
- TG-47 (IndexNow) will be active from Plan 01
- Supabase blog_posts table has: slug, title, content, meta_description, category, published_at, ai_generated
- Supabase URL: https://lwtmvzhwekgdxkaisfra.supabase.co
- Supabase anon key: sb_publishable_DeX21ldoDKl3NyJeFZzR1w_eWBUkx-v
- Telegram credential ID: V404qLIDXjmyzNeS
- n8n API base: https://tgyardcare.app.n8n.cloud
- Claude Sonnet for content generation (same as TG-99)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update TG-50 content refresher workflow JSON</name>
  <files>
    automation/n8n-workflows/TG-50-content-refresher.json
  </files>
  <action>
    Read TG-50 workflow JSON fully and update:

    1. **Schedule**: Weekly on Saturdays (e.g., `0 14 * * 6` for 2pm UTC / 9am CT Saturday)
       - Runs weekly, not daily — content refresh is lower priority than publishing

    2. **Stale detection logic**:
       - Query Supabase blog_posts for posts where `published_at < NOW() - INTERVAL '90 days'`
       - Also query seo_rankings/gsc_performance (from Plan 02) to prioritize: refresh posts that are BOTH stale AND losing rank first
       - If no rank data available yet (early days), fall back to pure age-based ordering

    3. **Refresh pipeline** (per stale post, max 3 per run to stay within Claude API budget):
       - Read existing post content from Supabase
       - Send to Claude Sonnet with prompt: "Update this lawn care blog post for [current season]. Add new information, update statistics, refresh examples. Keep the same slug and general topic. Maintain 1200-1500 word count. Include 2-3 internal links to tgyardcare.com service and city pages."
       - Quality gates same as TG-99: word count >= 800, title <= 65 chars, meta <= 155 chars
       - Update the post in Supabase: new content, updated meta_description, set published_at to NOW(), add `last_refreshed_at` timestamp
       - Call TG-47 IndexNow to re-submit the refreshed URL

    4. **Telegram summary** (end of workflow):
       - Send weekly summary: "CONTENT REFRESH: Found [N] stale posts. Refreshed [M] this week. Next candidates: [list top 3 remaining stale titles]"
       - Use Telegram credential V404qLIDXjmyzNeS

    5. **Cleanup**:
       - Fix any Supabase URL issues (no duplicate https://, replace placeholder project IDs)
       - Add automation_runs logging
       - Add error handling for Claude API failures (skip post, log error, continue to next)

    Save updated JSON locally.
  </action>
  <verify>
    - `grep -c "SUPABASE_PROJECT_ID" automation/n8n-workflows/TG-50-content-refresher.json` returns 0
    - `grep -c "https://https://" automation/n8n-workflows/TG-50-content-refresher.json` returns 0
    - `grep "90\|stale\|refresh" automation/n8n-workflows/TG-50-content-refresher.json` shows staleness logic
    - `grep "TG-47\|indexnow\|IndexNow" automation/n8n-workflows/TG-50-content-refresher.json` shows IndexNow integration
    - `grep "V404qLIDXjmyzNeS\|telegram" automation/n8n-workflows/TG-50-content-refresher.json` shows Telegram summary
    - JSON parses without errors
  </verify>
  <done>TG-50 workflow JSON is clean, configured with 90-day staleness threshold, rank-prioritized refresh order, max 3 posts/run, IndexNow re-submission, and weekly Telegram summary.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy TG-50 to n8n and activate</name>
  <files>
    automation/n8n-workflows/TG-50-content-refresher.json
  </files>
  <action>
    Deploy TG-50 to n8n using the API:

    1. Check if TG-50 already exists on n8n by listing workflows
    2. If exists: PUT to update, if not: POST to create
    3. Activate via POST /api/v1/workflows/{id}/activate
    4. If API PUT fails with 400, note for UI import

    After deployment, do a final audit of all Phase 16 workflows:
    - List all active workflows on n8n
    - Confirm TG-99, TG-103, TG-47, TG-96, TG-45, TG-97, TG-50 are all active
    - Count total active workflows (should be ~22: 15 pre-phase + 7 new)

    Create a verification summary showing all 8 SEO requirements mapped to their workflow status.
  </action>
  <verify>
    - n8n API GET /api/v1/workflows returns TG-50 in active state
    - Total active workflow count is ~22
    - All 7 Phase 16 workflows appear in active list
  </verify>
  <done>TG-50 active (weekly Saturday content refresh), all 7 SEO workflows deployed and active, Phase 16 complete with all 8 requirements covered.</done>
</task>

</tasks>

<verification>
- TG-50 scheduled weekly on Saturdays
- Stale detection uses 90-day threshold
- Refresh prioritizes rank-declining posts
- Max 3 posts refreshed per run
- Refreshed posts re-submitted to IndexNow via TG-47
- Weekly Telegram summary sent after each run
- All 7 Phase 16 workflows active on n8n
</verification>

<success_criteria>
- SEO-07: TG-50 active, detecting stale posts > 90 days, AI-refreshing up to 3/week, re-submitting to IndexNow
- Phase 16 complete: All 8 requirements (SEO-01 through SEO-08) covered by 7 active workflows
</success_criteria>

<output>
After completion, create `.planning/phases/16-content-seo-engine/16-03-SUMMARY.md`
</output>
