---
phase: 12-conversion-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - automation/n8n-workflows/TG-01-lead-capture.json
autonomous: true

must_haves:
  truths:
    - "Quote form submission triggers instant SMS to customer within 5 seconds"
    - "Quote form submission triggers instant Telegram alert to Vance with one-tap call link"
    - "Quote form submission triggers branded email confirmation to customer"
    - "All three channels fire in parallel after lead is saved to Supabase"
    - "Existing lead capture flow (normalize, score, insert, Brevo sync, webhook response) remains intact"
  artifacts:
    - path: "automation/n8n-workflows/TG-01-lead-capture.json"
      provides: "Enhanced lead capture with instant multi-channel response"
      contains: "Send SMS via TG-94"
  key_links:
    - from: "TG-01 Extract Lead ID node"
      to: "TG-94 via executeWorkflow"
      via: "executeWorkflow with workflowId AprqI2DgQA8lehij"
      pattern: "AprqI2DgQA8lehij"
    - from: "TG-01 Extract Lead ID node"
      to: "TG-95 via executeWorkflow"
      via: "executeWorkflow with workflowId IUDLrQrAkcLFLsIC"
      pattern: "IUDLrQrAkcLFLsIC"
    - from: "TG-01 Extract Lead ID node"
      to: "Telegram sendMessage"
      via: "n8n-nodes-base.telegram node with credential id telegram"
      pattern: "telegramApi"
---

<objective>
Enhance TG-01-lead-capture to fire instant multi-channel responses (SMS + Telegram + email) after every quote form submission.

Purpose: CONV-01, CONV-02, CONV-03 — nobody waits more than 5 seconds after submitting a quote form. Vance gets an instant Telegram alert with tap-to-call. Customer gets instant SMS + branded email confirmation.

Output: Updated TG-01-lead-capture.json with 3 new parallel branches after "Extract Lead ID" node.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@automation/n8n-workflows/TG-01-lead-capture.json
@automation/n8n-workflows/TG-94-unified-sms-sender.json
@automation/n8n-workflows/TG-95-unified-email-sender.json
@automation/n8n-workflows/TG-113-critical-alert-router.json
@automation/n8n-workflows/TG-74-telegram-bot.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add instant multi-channel response nodes to TG-01</name>
  <files>automation/n8n-workflows/TG-01-lead-capture.json</files>
  <action>
Modify the existing TG-01-lead-capture.json to add 3 NEW parallel branches that fire AFTER "Extract Lead ID" (which outputs the full lead data including lead_id, first_name, phone, email, service, lead_score, lead_tier).

The existing flow is:
  Webhook → Normalize → Score → Insert Supabase → Extract Lead ID → Sync Brevo → Build Response

After "Extract Lead ID", ADD these parallel branches (alongside the existing Sync Brevo path):

**Branch 1: Instant SMS via TG-94**
- Node: "Build SMS Body" (code node, typeVersion 2)
  - Position: [1100, -200]
  - Code: Build a friendly SMS message:
    ```
    const lead = $input.first().json;
    const name = lead.first_name || 'there';
    const service = lead.service || 'yard care';
    const msg = `Hi ${name}! Thanks for requesting a ${service} quote from TotalGuard Yard Care. We'll get back to you ASAP — usually within the hour! Questions? Call us: (608) 535-6057`;
    return [{ json: { to_phone: lead.phone, message_body: msg, workflow_name: 'TG-01', message_type: 'lead_confirmation' } }];
    ```
  - Only proceed if lead.phone is not empty (add an IF node "Has Phone?" before this, checking `$json.phone` is not empty string)

- Node: "IF Has Phone" (if node, typeVersion 2.2)
  - Position: [1100, -300]
  - Condition: `$json.phone` is not empty (string, isNotEmpty)
  - TRUE path → "Build SMS Body"

- Node: "Send SMS via TG-94" (executeWorkflow, typeVersion 1.1)
  - Position: [1320, -200]
  - workflowId: "AprqI2DgQA8lehij"
  - workflowInputs: to_phone, message_body, workflow_name, message_type from $json

**Branch 2: Instant Telegram Alert**
- Node: "Build Telegram Alert" (code node, typeVersion 2)
  - Position: [1100, -500]
  - Code: Build a rich Telegram message with tap-to-call link:
    ```
    const lead = $input.first().json;
    const name = `${lead.first_name || ''} ${lead.last_name || ''}`.trim() || 'Unknown';
    const phone = lead.phone || 'No phone';
    const email = lead.email || 'No email';
    const service = lead.service || 'Not specified';
    const source = lead.source || 'website';
    const score = lead.lead_score || 0;
    const tier = lead.lead_tier || 'unknown';
    const tierEmoji = tier === 'hot' ? '🔥' : tier === 'warm' ? '🟡' : '🔵';
    const time = new Date().toLocaleString('en-US', { timeZone: 'America/Chicago', hour: 'numeric', minute: '2-digit', hour12: true });

    const msg = `🚨 NEW LEAD\n\n👤 ${name}\n📱 ${phone}\n📧 ${email}\n🔧 ${service}\n📍 Source: ${source}\n${tierEmoji} Score: ${score} (${tier})\n⏰ ${time}\n\n📞 Tap to call: tel:${phone.replace(/[^+\d]/g, '')}`;

    return [{ json: { chatId: 'OWNER_TELEGRAM_CHAT_ID', text: msg } }];
    ```
  - IMPORTANT: Use placeholder `OWNER_TELEGRAM_CHAT_ID` — the executor will need to replace this with Vance's actual Telegram chat ID. The executor should get this from a test message to the bot or from n8n environment variables.

- Node: "Send Telegram Alert" (n8n-nodes-base.telegram, typeVersion 1.2)
  - Position: [1320, -500]
  - chatId: `={{$json.chatId}}`
  - text: `={{$json.text}}`
  - additionalFields: { parse_mode: "HTML" }  (actually use no parse_mode since we're using plain text with emojis — or use Markdown)
  - credentials: { telegramApi: { id: "telegram", name: "Telegram" } }

**Branch 3: Instant Email Confirmation via TG-95**
- Node: "Build Confirmation Email" (code node, typeVersion 2)
  - Position: [1100, -700]
  - Code: Build a branded HTML email confirmation. Use the SAME email template style from TG-09 (green #2D5A27 header, gold #C5A55A accents, TotalGuard branding, footer with address). Content:
    - Subject: "We received your quote request, {first_name}!"
    - Headline: "Your Quote Request is Confirmed"
    - Body: Thank them for reaching out about {service}. Mention someone will be in touch within the hour. Include phone number (608) 535-6057 to call directly. Include trust signals (4.9 star rating, 500+ properties, licensed & insured).
    - CTA button: "Visit Our Website" → https://tgyardcare.com
    - Preview text: "Thanks for choosing TotalGuard! We'll be in touch shortly."
  - Output: `{ to_email, subject, html_body, workflow_name: 'TG-01', email_type: 'lead_confirmation' }`

- Node: "IF Has Email" (if node, typeVersion 2.2)
  - Position: [1100, -800]
  - Condition: `$json.email` is not empty
  - TRUE path → "Build Confirmation Email"

- Node: "Send Email via TG-95" (executeWorkflow, typeVersion 1.1)
  - Position: [1320, -700]
  - workflowId: "IUDLrQrAkcLFLsIC"
  - workflowInputs: to_email, subject, html_body, workflow_name, email_type from $json

**Connections update:**
The "Extract Lead ID" node currently connects only to "Sync Brevo Contact". Update to connect to ALL of:
1. "Sync Brevo Contact" (existing, index 0)
2. "IF Has Phone" (new, same output index 0 — n8n supports multiple connections from one output)
3. "Build Telegram Alert" (new, same output index 0)
4. "IF Has Email" (new, same output index 0)

Then:
- "IF Has Phone" TRUE → "Build SMS Body" → "Send SMS via TG-94"
- "Build Telegram Alert" → "Send Telegram Alert"
- "IF Has Email" TRUE → "Build Confirmation Email" → "Send Email via TG-95"

The "Build Response" node should still connect from "Sync Brevo Contact" only (the webhook response doesn't wait for SMS/Telegram/email to complete — they fire in parallel).

**Node IDs to use:**
- if-has-phone, build-sms-body, send-sms-tg94
- build-telegram-alert, send-telegram-alert
- if-has-email, build-confirmation-email, send-email-tg95

IMPORTANT: Do NOT change any existing node IDs, positions, or logic. Only ADD new nodes and update the connections from "Extract Lead ID".

IMPORTANT: Use n8n variable references (`$vars.`) for Supabase URLs/keys where applicable. For Telegram credential, use `{ "telegramApi": { "id": "telegram", "name": "Telegram" } }` matching TG-74's pattern.

IMPORTANT: The HTML email builder function should be included inline in the "Build Confirmation Email" code node. Copy the buildEmailHtml pattern from TG-09 (the branded template with green header, gold accents, trust strip, dark footer).
  </action>
  <verify>
1. `cat automation/n8n-workflows/TG-01-lead-capture.json | python -m json.tool > /dev/null` — valid JSON
2. Verify nodes array contains all original nodes PLUS: if-has-phone, build-sms-body, send-sms-tg94, build-telegram-alert, send-telegram-alert, if-has-email, build-confirmation-email, send-email-tg95
3. Verify connections: "Extract Lead ID" connects to "Sync Brevo Contact" AND "IF Has Phone" AND "Build Telegram Alert" AND "IF Has Email"
4. Verify "Send SMS via TG-94" has workflowId "AprqI2DgQA8lehij"
5. Verify "Send Email via TG-95" has workflowId "IUDLrQrAkcLFLsIC"
6. Verify Telegram node uses credential `{ "telegramApi": { "id": "telegram", "name": "Telegram" } }`
7. Verify no existing nodes were modified (same IDs, positions, logic)
  </verify>
  <done>
TG-01-lead-capture.json contains 3 parallel instant-response branches (SMS, Telegram, email) that fire after lead insertion. All branches use correct workflow IDs and credential references. Existing lead capture flow untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy updated TG-01 to n8n and verify activation</name>
  <files>automation/n8n-workflows/TG-01-lead-capture.json</files>
  <action>
Deploy the updated TG-01 workflow to n8n cloud using the API.

1. First, get TG-01's workflow ID from n8n:
   ```
   curl -s -H "X-N8N-API-KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI5NjkyNjY2OC00OGI3LTQwYmMtOGM4My01ZDA5OWY4MTc5MjUiLCJpc3MiOiJuOG4iLCJhdWQiOiJwdWJsaWMtYXBpIiwianRpIjoiZmIzMjUzM2EtYjYzMS00YmQzLWJlMzAtMmMzMmQ4MGVjYmUyIiwiaWF0IjoxNzczNjQ3NzE0fQ.W5pIrCxz4mtuVmaJrJqUEqdNl0bRjKDKdVRrkWQlsOA" \
     "https://tgyardcare.app.n8n.cloud/api/v1/workflows?tags=fPXl9eiMhJc3ISQQ&limit=200" | python -m json.tool | grep -A2 "TG-01"
   ```
   (Or search by name if needed)

2. Update the workflow via PUT:
   ```
   curl -s -X PUT -H "X-N8N-API-KEY: ..." -H "Content-Type: application/json" \
     -d @automation/n8n-workflows/TG-01-lead-capture.json \
     "https://tgyardcare.app.n8n.cloud/api/v1/workflows/{WORKFLOW_ID}"
   ```
   IMPORTANT: The PUT body MUST include the "name" field (already present in the JSON).

3. Activate the workflow:
   ```
   curl -s -X PATCH -H "X-N8N-API-KEY: ..." -H "Content-Type: application/json" \
     -d '{"active": true}' \
     "https://tgyardcare.app.n8n.cloud/api/v1/workflows/{WORKFLOW_ID}"
   ```

4. Verify activation:
   ```
   curl -s -H "X-N8N-API-KEY: ..." \
     "https://tgyardcare.app.n8n.cloud/api/v1/workflows/{WORKFLOW_ID}" | python -m json.tool | grep '"active"'
   ```
   Should show `"active": true`

IMPORTANT: If deployment fails with 400/500, check the error message. Common issues:
- Sub-workflow not found: TG-94 or TG-95 may need to be active first (they should be from Phase 11)
- Invalid credential: The Telegram credential ID must match what's in n8n
- Missing "name" field in PUT body

IMPORTANT: Regarding OWNER_TELEGRAM_CHAT_ID — to find Vance's chat ID, check if there's a TG_OWNER_CHAT_ID variable in n8n, or check TG-74's execution logs for the chatId value. If neither available, leave the placeholder and note it as a user_setup item. The Telegram alert will fail silently until the chat ID is set, but SMS and email will still work.

Rate limit awareness: This task uses ~4 API calls. Stay within the ~10 calls/session budget.
  </action>
  <verify>
1. n8n API returns `"active": true` for TG-01
2. The workflow has all new nodes visible in the response
3. No activation errors in the response
  </verify>
  <done>
TG-01 is deployed to n8n with all 3 instant-response branches active. Quote form submissions will now trigger parallel SMS + Telegram + email within seconds.
  </done>
</task>

</tasks>

<verification>
- TG-01-lead-capture.json is valid JSON with all original + new nodes
- Connections show "Extract Lead ID" fanning out to 4 parallel paths
- TG-94 workflow ID (AprqI2DgQA8lehij) correctly referenced in SMS branch
- TG-95 workflow ID (IUDLrQrAkcLFLsIC) correctly referenced in email branch
- Telegram credential matches TG-74 pattern (id: "telegram", name: "Telegram")
- Workflow is active on n8n cloud
</verification>

<success_criteria>
- CONV-01: SMS branch calls TG-94 with lead phone + confirmation message
- CONV-02: Telegram branch sends alert with lead details + tap-to-call link
- CONV-03: Email branch calls TG-95 with branded HTML confirmation
- All 3 branches fire in parallel after "Extract Lead ID"
- Existing lead capture flow (normalize → score → insert → Brevo → response) untouched
- Workflow deployed and active on n8n
</success_criteria>

<output>
After completion, create `.planning/phases/12-conversion-engine/12-01-SUMMARY.md`
</output>
