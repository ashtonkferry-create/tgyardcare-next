---
phase: 05-safety-nets-foundation
plan: 07
type: execute
wave: 4
depends_on: ["01", "02", "03", "04", "05", "06"]
files_modified:
  - .planning/phases/05-safety-nets-foundation/05-07-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "Homepage loads with zero errors in browser DevTools console"
    - "Animated stat counters display correct values on first load"
    - "All headings render in Clash Display, body in General Sans"
    - "Light text on dark backgrounds passes WCAG AA contrast"
    - "tsc --noEmit error budget is established"
  artifacts:
    - path: ".planning/phases/05-safety-nets-foundation/05-07-SUMMARY.md"
      provides: "Phase 5 final verification report with pass/fail for all 12 checks"
      contains: "PASS"
  key_links: []
---

<objective>
Final verification of all Phase 5 success criteria.

Purpose: This plan runs all verification checks to confirm the phase goal is met before marking Phase 5 complete. It covers console errors, fonts, stats, contrast, TypeScript budget, smoke tests, and security items.

Output: Verification report confirming all 5 success criteria pass or documenting remaining gaps.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-safety-nets-foundation/05-01-SUMMARY.md
@.planning/phases/05-safety-nets-foundation/05-02-SUMMARY.md
@.planning/phases/05-safety-nets-foundation/05-03-SUMMARY.md
@.planning/phases/05-safety-nets-foundation/05-04-SUMMARY.md
@.planning/phases/05-safety-nets-foundation/05-05-SUMMARY.md
@.planning/phases/05-safety-nets-foundation/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated verification checks</name>
  <files>.planning/phases/05-safety-nets-foundation/05-07-SUMMARY.md</files>
  <action>
Run each verification in sequence and record results:

1. **Build check:** `npm run build` — must succeed
2. **TypeScript budget:** `npx tsc --noEmit 2>&1 | grep -c "error TS"` — must match .tsc-error-budget.json baseline
3. **No Inter font:** `grep -rn "Inter\|fonts.googleapis.com" src/app/layout.tsx` — must return empty
4. **Font files exist:** `ls -la src/app/fonts/*.woff2` — both files must exist and be >20KB
5. **Font CSS variables:** `grep -n "font-display\|font-body" src/app/globals.css` — must show variables
6. **Brand name:** `grep -rn "TG Yard Care" src/app/layout.tsx` — must return empty
7. **Seasonal CSS:** `grep -n "season-accent" src/app/globals.css` — must show variables
8. **Supabase SSR clients:** `ls src/lib/supabase/server.ts src/lib/supabase/client.ts` — both must exist
9. **Auth middleware:** `grep -n "portal\|admin.*login" src/middleware.ts` — must show auth logic
10. **Smoke tests:** `npx playwright test --list` — must list 10 tests
11. **Cron audit:** `wc -l docs/cron-audit.md` — must have content
12. **Error budget file:** `cat .tsc-error-budget.json | head -5` — must have valid JSON

Record pass/fail for each. If any fail, note what's missing.
  </action>
  <verify>
1. `npm run build` — exits 0
2. `npx tsc --noEmit 2>&1 | grep -c "error TS"` — count matches .tsc-error-budget.json baseline
3. `grep -rn "Inter" src/app/layout.tsx` — returns no matches
4. `ls src/app/fonts/*.woff2` — both files exist
5. `grep -n "font-display" src/app/globals.css` — returns matches
6. `grep -rn "TG Yard Care" src/app/layout.tsx` — returns no matches
7. `grep -n "season-accent" src/app/globals.css` — returns matches
8. `ls src/lib/supabase/server.ts src/lib/supabase/client.ts` — both exist
9. `grep -n "portal\|admin.*login" src/middleware.ts` — returns matches
10. `npx playwright test --list` — lists 10 tests
11. `ls docs/cron-audit.md` — file exists
12. `cat .tsc-error-budget.json | head -5` — valid JSON with errorCount
  </verify>
  <done>All automated verification checks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 5 Safety Nets & Foundation — complete hardening pass:
- Supabase console errors fixed (silent fallback)
- Inter font replaced with Clash Display + General Sans (self-hosted)
- TypeScript error budget established
- @supabase/ssr server + browser clients created
- Auth middleware for /portal/* and /admin/*
- Seasonal CSS variables consolidated
- Brand name unified to "TotalGuard"
- Playwright smoke tests for 10 public pages
- All cron jobs audited and documented
  </what-built>
  <how-to-verify>
1. Visit https://tgyardcare.com (or local dev) — open DevTools Console tab — should see zero Supabase errors
2. Inspect headings — should be Clash Display font (check computed styles)
3. Inspect body text — should be General Sans font
4. Check homepage stat counters — should show 4.9, 80+, 100%, 24hr (not 0)
5. Check page title in browser tab — should say "TotalGuard Yard Care" not "TG Yard Care"
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 5 complete, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 5 phase success criteria verified:
1. Zero console errors on homepage
2. Correct stat counter values
3. Clash Display headings, General Sans body
4. WCAG AA contrast on dark sections
5. TypeScript error budget established
</verification>

<success_criteria>
- All 12 automated checks pass
- Human visual verification confirms fonts, stats, and console cleanliness
- Phase 5 can be marked COMPLETE
</success_criteria>

<output>
After completion, create `.planning/phases/05-safety-nets-foundation/05-07-SUMMARY.md`
</output>
