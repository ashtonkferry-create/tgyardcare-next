---
phase: 11-foundation-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - automation/n8n-workflows/*.json
autonomous: true

must_haves:
  truths:
    - "No local workflow JSON file contains a real Supabase secret key"
    - "All Supabase API references in local files use the placeholder SUPABASE_SECRET_KEY"
    - "An audit report confirms the key hygiene status of all 127 workflow files"
  artifacts:
    - path: "automation/n8n-workflows/KEY_AUDIT.md"
      provides: "Audit results documenting key hygiene across all workflow JSONs"
      min_lines: 10
  key_links: []
---

<objective>
Audit all 127 local workflow JSON files in `automation/n8n-workflows/` to confirm none contain real Supabase secret keys, and document the audit results.

Purpose: The Supabase secret key was exposed on GitHub on 2026-03-15 and rotated. Local workflow JSONs should use the placeholder `SUPABASE_SECRET_KEY` (never the real key). This audit confirms that policy is enforced and creates a record for future reference. If any files contain the old or current real key, they must be sanitized.

Output: Audit report confirming all files are clean, or files fixed if any contain real keys.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all workflow JSONs for leaked keys and produce report</name>
  <files>
    automation/n8n-workflows/*.json
    automation/n8n-workflows/KEY_AUDIT.md
  </files>
  <action>
**Step 1: Scan for real keys.**

Search all 127 JSON files in `automation/n8n-workflows/` for:
- The OLD revoked key: `SUPABASE_SECRET_KEY_REVOKED`
- The CURRENT valid key: `SUPABASE_SECRET_KEY_CURRENT`
- Any other `sb_secret_` pattern that is NOT the placeholder `SUPABASE_SECRET_KEY`

Use grep/ripgrep to scan all files. The pattern to search: `sb_secret_[A-Za-z0-9_]+` (matches real keys but not the word "SUPABASE_SECRET_KEY").

**Step 2: If any files contain real keys -- fix them.**

Replace any found real key with the placeholder string `SUPABASE_SECRET_KEY`. This is the convention established in the project (see `reference_supabase_tgyardcare.md`: "Never commit real key to git -- use placeholder").

**Step 3: Count files that reference Supabase.**

Count how many of the 127 workflow files contain:
- `SUPABASE_SECRET_KEY` (the placeholder) -- expected in many workflows
- `supabase.co` (the Supabase URL) -- expected in workflows that call Supabase REST API
- Neither -- workflows that don't interact with Supabase

**Step 4: Create audit report.**

Create `automation/n8n-workflows/KEY_AUDIT.md` with:
- Date of audit
- Total files scanned
- Files with placeholder (clean)
- Files with real keys found and fixed (if any)
- Files with no Supabase references
- Conclusion: PASS or FAIL with remediation notes

**Important:** Do NOT commit real keys to git. If any file had a real key and was fixed, mention it was fixed but do not include the key value in the audit report.
  </action>
  <verify>
Run these commands and confirm:
```bash
# Zero real keys in any workflow file
grep -r "sb_secret_[A-Za-z0-9]" automation/n8n-workflows/*.json | grep -v "SUPABASE_SECRET_KEY" | wc -l
# Expected: 0

# Audit report exists
cat automation/n8n-workflows/KEY_AUDIT.md
```
  </verify>
  <done>
All 127 workflow JSON files confirmed clean -- zero real Supabase keys present. Audit report exists at `automation/n8n-workflows/KEY_AUDIT.md` documenting results. INFRA-04 requirement satisfied.
  </done>
</task>

</tasks>

<verification>
- `grep -r "sb_secret_[A-Za-z0-9]" automation/n8n-workflows/*.json | grep -v SUPABASE_SECRET_KEY` returns zero results
- `KEY_AUDIT.md` exists and documents the audit
- No real keys committed to git
</verification>

<success_criteria>
- Zero real Supabase keys in any local workflow JSON
- All Supabase references use placeholder `SUPABASE_SECRET_KEY`
- Audit report created and committed
- INFRA-04 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation-infrastructure/11-02-SUMMARY.md`
</output>
