---
phase: 11-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "TG-92 webhook router is active in n8n and responds to incoming webhooks"
    - "TG-113 critical alert router is active in n8n and can be called by other workflows"
    - "Both workflows use the current valid Supabase secret key, not the revoked one"
  artifacts: []
  key_links:
    - from: "TG-92 HTTP Request nodes"
      to: "Supabase REST API"
      via: "apikey + Authorization headers"
      pattern: "SUPABASE_SECRET_KEY_CURRENT"
---

<objective>
Fix TG-92 (webhook router) and TG-113 (critical alert router) live workflows in n8n by replacing stale Supabase keys with the current valid key, then activate both workflows.

Purpose: These two workflows return HTTP 400 when activated because they contain the revoked Supabase key `SUPABASE_SECRET_KEY_REVOKED`. TG-92 is the central webhook router that handles all inbound webhooks (Twilio SMS, web forms). TG-113 routes critical alerts via email and SMS. Both are required infrastructure for all downstream M4 phases.

Output: Both workflows active and using current Supabase key.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch, patch, and activate TG-92 and TG-113 via n8n API</name>
  <files>No local files modified -- this is all n8n API work</files>
  <action>
Use the n8n REST API to fix both workflows. All calls go to `https://tgyardcare.app.n8n.cloud/api/v1/` with header `X-N8N-API-KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI5NjkyNjY2OC00OGI3LTQwYmMtOGM4My01ZDA5OWY4MTc5MjUiLCJpc3MiOiJuOG4iLCJhdWQiOiJwdWJsaWMtYXBpIiwianRpIjoiZmIzMjUzM2EtYjYzMS00YmQzLWJlMzAtMmMzMmQ4MGVjYmUyIiwiaWF0IjoxNzczNjQ3NzE0fQ.W5pIrCxz4mtuVmaJrJqUEqdNl0bRjKDKdVRrkWQlsOA`.

**Step 1: Find workflow IDs for TG-92 and TG-113.**
- GET `/api/v1/workflows` and search response for workflows named `TG-92` and `TG-113` (or containing those strings).
- Record their n8n workflow IDs.

**Step 2: For TG-92 -- fetch full workflow JSON.**
- GET `/api/v1/workflows/{TG-92-ID}`
- The workflow JSON contains multiple HTTP Request nodes that use `SUPABASE_SECRET_KEY` as the placeholder in headerParameters. However, the LIVE version in n8n likely has the OLD key `SUPABASE_SECRET_KEY_REVOKED` hardcoded instead of the placeholder.
- In the response JSON, find ALL occurrences of the old key OR the placeholder `SUPABASE_SECRET_KEY` in header parameter values.
- Replace ALL with the current valid key: `SUPABASE_SECRET_KEY_CURRENT`
- PUT the updated workflow back via `/api/v1/workflows/{TG-92-ID}` with the corrected nodes array.

**Step 3: Activate TG-92.**
- POST `/api/v1/workflows/{TG-92-ID}/activate`
- Confirm 200 response with `"active": true`.

**Step 4: For TG-113 -- fetch and inspect.**
- GET `/api/v1/workflows/{TG-113-ID}`
- TG-113 may NOT contain Supabase HTTP requests (local JSON shows it only uses executeWorkflow nodes to call TG-95 and TG-94). The HTTP 400 error may have a different root cause.
- If TG-113 contains the stale Supabase key: replace it the same way as TG-92.
- If TG-113 does NOT contain the stale key: the activation error has a different cause. Inspect the workflow JSON for issues (invalid credential references, missing node types, etc.) and fix accordingly.

**Step 5: Activate TG-113.**
- POST `/api/v1/workflows/{TG-113-ID}/activate`
- Confirm 200 response with `"active": true`.

**Step 6: Verify both are active.**
- GET `/api/v1/workflows/{TG-92-ID}` and confirm `"active": true`
- GET `/api/v1/workflows/{TG-113-ID}` and confirm `"active": true`

**Important notes:**
- Use `curl` for all API calls.
- The n8n API uses `Content-Type: application/json` for PUT requests.
- When PUTting the updated workflow, you must include the full `nodes` array in the request body. You can send just `{ "nodes": [...updated nodes...] }` -- the n8n API merges partial updates.
- Do NOT change any workflow logic, connections, or settings -- only replace the Supabase key values in HTTP Request node headers.
  </action>
  <verify>
Run these curl commands and confirm both return `"active": true`:
```
curl -s -H "X-N8N-API-KEY: ..." https://tgyardcare.app.n8n.cloud/api/v1/workflows/{TG-92-ID} | grep '"active"'
curl -s -H "X-N8N-API-KEY: ..." https://tgyardcare.app.n8n.cloud/api/v1/workflows/{TG-113-ID} | grep '"active"'
```
Also grep the TG-92 workflow response for the old key to confirm it is gone:
```
curl -s -H "X-N8N-API-KEY: ..." https://tgyardcare.app.n8n.cloud/api/v1/workflows/{TG-92-ID} | grep -c "QlNjUO"
```
Expected: 0 occurrences.
  </verify>
  <done>
TG-92 is active with all Supabase header values set to `SUPABASE_SECRET_KEY_CURRENT`. TG-113 is active. Both return `"active": true` from the n8n API. No occurrences of the old revoked key remain in either workflow.
  </done>
</task>

</tasks>

<verification>
- GET both workflow IDs from n8n API and confirm `"active": true`
- Grep TG-92 response body for old key `QlNjUO` -- must return 0 matches
- Grep TG-92 response body for new key `mWHIW` -- must return matches in all HTTP Request header nodes
- TG-113 active and callable by other workflows
</verification>

<success_criteria>
- TG-92 webhook router: active, using current Supabase key in all HTTP Request nodes
- TG-113 critical alert router: active
- Zero occurrences of revoked key `SUPABASE_SECRET_KEY_REVOKED` in either workflow
- INFRA-03 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/11-foundation-infrastructure/11-01-SUMMARY.md`
</output>
