---
phase: 07-conversion-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/geocoding.ts
  - src/data/daneCountyBoundaries.ts
autonomous: true

must_haves:
  truths:
    - "leaflet, react-leaflet, and @types/leaflet are installed and importable"
    - "Server action searchAddresses returns structured address suggestions from Mapbox API"
    - "Static GeoJSON data exists for all 12 Dane County service cities with correct [lat,lng] polygon coordinates"
    - "leads table has a selected_services jsonb column for multi-service annual plan data"
  artifacts:
    - path: "src/lib/geocoding.ts"
      provides: "Mapbox Geocoding v6 server action"
      contains: "use server"
    - path: "src/data/daneCountyBoundaries.ts"
      provides: "Static city boundary polygons for Leaflet map"
      exports: ["CITY_BOUNDARIES"]
  key_links:
    - from: "src/lib/geocoding.ts"
      to: "https://api.mapbox.com/search/geocode/v6/forward"
      via: "fetch with MAPBOX_ACCESS_TOKEN from env"
      pattern: "process\\.env\\.MAPBOX_ACCESS_TOKEN"
---

<objective>
Install map dependencies, create the Mapbox geocoding server action, build the static city boundary GeoJSON data file, and add the `selected_services` column to the leads table.

Purpose: All three conversion features (quote autocomplete, interactive map, annual plan configurator) depend on these shared data/infra pieces. Building them first unblocks parallel execution of Plans 02, 03, and 04.

Output: 2 new source files (geocoding.ts, daneCountyBoundaries.ts), updated package.json, DB migration applied.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-conversion-features/07-RESEARCH.md
@src/hooks/useLocations.ts
@src/hooks/usePricing.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install leaflet packages + create Mapbox geocoding server action</name>
  <files>
    package.json
    src/lib/geocoding.ts
  </files>
  <action>
  1. Install packages:
     ```bash
     npm install leaflet react-leaflet
     npm install -D @types/leaflet
     ```

  2. Create `src/lib/geocoding.ts` as a Next.js server action file:
     - Add `'use server'` directive at top
     - Export `searchAddresses(query: string)` function
     - Call Mapbox Geocoding v6 endpoint: `https://api.mapbox.com/search/geocode/v6/forward`
     - Read `process.env.MAPBOX_ACCESS_TOKEN` (NEVER expose to client)
     - Parameters: `q=query`, `access_token=token`, `country=US`, `types=address`, `proximity=-89.401,43.073` (Madison WI bias), `autocomplete=true`, `limit=5`
     - Return type: `AddressSuggestion[]` where each has `place_name: string`, `address: string`, `city: string`, `state: string`, `zip: string`, `coordinates: [number, number]` (as [lng, lat] from Mapbox)
     - Parse Mapbox v6 response: each feature has `properties.full_address`, `properties.context.place.name` (city), `properties.context.postcode.name` (zip), `properties.context.region.region_code` (state abbrev), `geometry.coordinates` ([lng, lat])
     - If MAPBOX_ACCESS_TOKEN is not set, return empty array (graceful degradation, no crash)
     - Guard: if query.length < 3, return empty array immediately (don't call API)

  IMPORTANT: TypeScript error budget is 84/85. This file must add ZERO TypeScript errors. Type all parameters and return values explicitly. Do NOT use `any`.
  </action>
  <verify>
  - `npm ls leaflet react-leaflet` shows both installed
  - `npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 84 or less (no new errors)
  - `grep -c "use server" src/lib/geocoding.ts` returns 1
  - `grep "MAPBOX_ACCESS_TOKEN" src/lib/geocoding.ts` confirms env var usage (not hardcoded)
  </verify>
  <done>leaflet + react-leaflet installed. geocoding.ts exports searchAddresses server action that calls Mapbox v6 with proper typing, env-based token, and Madison proximity bias.</done>
</task>

<task type="auto">
  <name>Task 2: Create Dane County city boundaries data + DB migration</name>
  <files>
    src/data/daneCountyBoundaries.ts
  </files>
  <action>
  1. Create `src/data/daneCountyBoundaries.ts` with static boundary data for all 12 service cities:
     Madison, Middleton, Monona, Sun Prairie, Fitchburg, Verona, McFarland, Cottage Grove, Waunakee, DeForest, Oregon, Stoughton

     Export interface and constant:
     ```typescript
     export interface CityBoundary {
       name: string;
       slug: string;
       coordinates: [number, number][][]; // Leaflet format: [lat, lng][][] (outer ring)
       center: [number, number]; // [lat, lng] for label placement
       color: string; // hex color for polygon fill
     }

     export const CITY_BOUNDARIES: CityBoundary[] = [...]
     ```

     For each city, use simplified polygon boundaries (8-15 points per city is sufficient for visual representation at zoom level 10-12). The coordinates MUST be in Leaflet format [lat, lng], NOT GeoJSON [lng, lat].

     Use these approximate centers (from existing locations table geo_lat/geo_lng):
     - Madison: [43.073, -89.401]
     - Middleton: [43.097, -89.504]
     - Monona: [43.062, -89.334]
     - Sun Prairie: [43.184, -89.214]
     - Fitchburg: [43.013, -89.441]
     - Verona: [42.991, -89.533]
     - McFarland: [43.012, -89.289]
     - Cottage Grove: [43.076, -89.199]
     - Waunakee: [43.192, -89.455]
     - DeForest: [43.248, -89.343]
     - Oregon: [42.926, -89.385]
     - Stoughton: [42.917, -89.218]

     Create simplified polygons by defining rectangular/hexagonal approximations around each center point. Use offsets of ~0.03-0.05 degrees (roughly 2-4 miles) to create visually distinct, non-overlapping city shapes. Use a distinct green-shade color palette (matching TotalGuard brand):
     - Use variations of emerald/green: #22c55e, #16a34a, #15803d, #059669, #0d9488, #14b8a6, #10b981, #34d399, #4ade80, #6ee7b7, #a7f3d0, #86efac

     Slug must match the locations table slug format (lowercase, hyphenated): madison, middleton, monona, sun-prairie, fitchburg, verona, mcfarland, cottage-grove, waunakee, deforest, oregon, stoughton.

  2. Run the Supabase migration to add `selected_services` column to leads table:
     ```sql
     ALTER TABLE leads ADD COLUMN IF NOT EXISTS selected_services jsonb;
     ```
     Execute via the Supabase Management API or `supabase db push` — use whichever method is established in this codebase (check for existing migration patterns in supabase/migrations/).

     If no migration tooling is set up, create the SQL file at `supabase/migrations/073_add_selected_services_to_leads.sql` and note it needs manual application.

  IMPORTANT: TypeScript error budget is 84/85. The daneCountyBoundaries.ts file must compile with zero errors. Type the coordinates array explicitly as `[number, number][][]`.
  </action>
  <verify>
  - `npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 84 or less
  - `grep "CITY_BOUNDARIES" src/data/daneCountyBoundaries.ts` confirms export exists
  - `grep -c "slug:" src/data/daneCountyBoundaries.ts` returns 12 (all cities present)
  - Migration SQL file exists or column confirmed added
  </verify>
  <done>Static boundary data for 12 cities exported as typed CITY_BOUNDARIES array with Leaflet-format [lat,lng] coordinates. leads.selected_services jsonb column added via migration.</done>
</task>

</tasks>

<verification>
- `npm ls leaflet react-leaflet @types/leaflet` — all three packages installed
- `npx tsc --noEmit 2>&1 | grep -c "error TS"` — still 84 or fewer errors (zero new)
- `node -e "const g = require('./src/lib/geocoding'); console.log(typeof g)"` — module loads without error
- `src/data/daneCountyBoundaries.ts` exports 12 city entries with coordinates arrays
- `src/lib/geocoding.ts` has 'use server' directive and uses MAPBOX_ACCESS_TOKEN env var
</verification>

<success_criteria>
- leaflet, react-leaflet, @types/leaflet installed in package.json
- geocoding.ts server action compiles, returns typed AddressSuggestion[]
- daneCountyBoundaries.ts has 12 cities with [lat,lng] polygon coordinates
- leads table has selected_services jsonb column
- TypeScript error count unchanged at 84
</success_criteria>

<output>
After completion, create `.planning/phases/07-conversion-features/07-01-SUMMARY.md`
</output>
