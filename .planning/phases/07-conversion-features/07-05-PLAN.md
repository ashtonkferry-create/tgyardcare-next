---
phase: 07-conversion-features
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "TypeScript error count is 84 or fewer (zero new errors from Phase 7)"
    - "Build succeeds without errors (npm run build passes)"
    - "Service areas page renders interactive map with 12 clickable city polygons"
    - "Quote flow starts with address autocomplete step, all 7 steps work"
    - "Annual plan configurator renders with service toggles, calendar, and pricing"
    - "All three conversion features are accessible from the homepage or relevant pages"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage with links/integration points for conversion features"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/AnnualPlanConfigurator.tsx"
      via: "import or link to plan builder page"
      pattern: "AnnualPlanConfigurator|annual-plan"
---

<objective>
Integrate the Annual Plan Configurator into an accessible page, add a CTA link from the homepage, run full TypeScript + build verification, and perform visual QA on all three conversion features.

Purpose: Final integration and verification. Ensures all conversion features are reachable, the TS error budget is maintained, and the build passes.

Output: Integrated configurator, verified build, visual QA checkpoint.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-conversion-features/07-RESEARCH.md
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate AnnualPlanConfigurator into a page + homepage CTA</name>
  <files>
    src/app/annual-plan/page.tsx
    src/app/page.tsx
  </files>
  <action>
  1. Create `src/app/annual-plan/page.tsx` as a page that renders the AnnualPlanConfigurator:
     - Server Component wrapper with metadata (title, description for SEO)
     - Import Navigation and Footer
     - Import AnnualPlanConfigurator via client island (it's already 'use client')
     - Add a hero section with heading "Build Your Custom Annual Lawn Care Plan" and subtext "Toggle services by season, see your price instantly, and lock in your plan for the year."
     - Render `<AnnualPlanConfigurator />` below the hero
     - Add JSON-LD schema (WebPage type) for the page
     - Style matching TotalGuard dark theme

  2. Add a CTA or link to the annual plan page from the homepage:
     - Find the appropriate section on `src/app/page.tsx` (likely near the services section or CTA section)
     - Add a card or button linking to `/annual-plan` with text like "Build Your Annual Plan" or "Save 15% with a Custom Plan"
     - Keep it subtle — don't disrupt existing homepage flow. A banner or card in an existing section is sufficient.
     - If the homepage has a CTA section (CTASection component), add the annual plan as a secondary CTA alongside existing ones

  IMPORTANT: TypeScript error budget is 84/85. Add ZERO new TS errors.
  IMPORTANT: The annual-plan page should follow the same Server Component + client island pattern established in Phase 6.
  </action>
  <verify>
  - `npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 84 or less
  - `src/app/annual-plan/page.tsx` exists and imports AnnualPlanConfigurator
  - Homepage has a link/CTA pointing to /annual-plan
  - `npm run build` succeeds
  </verify>
  <done>Annual plan configurator is accessible at /annual-plan with proper metadata and SEO. Homepage has a CTA linking to it. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Full build verification + TypeScript audit</name>
  <files></files>
  <action>
  Run comprehensive verification:

  1. TypeScript error count:
     ```bash
     npx tsc --noEmit 2>&1 | grep -c "error TS"
     ```
     Must be 84 or fewer. If higher than 84, identify and fix the new errors introduced by Phase 7 files.

  2. Full build:
     ```bash
     npm run build
     ```
     Must complete without errors. Pay special attention to:
     - Leaflet SSR issues (ServiceAreaMap must use next/dynamic with ssr:false)
     - Server action compilation (geocoding.ts with 'use server')
     - Missing imports or circular dependencies

  3. If any errors found, fix them:
     - TS errors in new files: add explicit types, use proper Leaflet type imports
     - Build errors from Leaflet SSR: ensure dynamic import is correctly configured
     - Missing env var errors: ensure geocoding.ts gracefully handles missing MAPBOX_ACCESS_TOKEN

  4. Verify all new files exist:
     - src/lib/geocoding.ts
     - src/data/daneCountyBoundaries.ts
     - src/components/AddressAutocomplete.tsx
     - src/components/ServiceAreaMap.tsx
     - src/components/AnnualPlanConfigurator.tsx
     - src/app/annual-plan/page.tsx
  </action>
  <verify>
  - `npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 84 or fewer
  - `npm run build` exits with code 0
  - All 6 new files exist and are importable
  </verify>
  <done>Full build passes, TypeScript error count at or below 84, all Phase 7 files compile correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Three conversion features: (1) Interactive service area map with 12 clickable city polygons on /service-areas, (2) Address autocomplete as first step of quote flow on /get-quote, (3) Annual plan configurator at /annual-plan with service/season toggles, calendar view, and bundle pricing.</what-built>
  <how-to-verify>
  1. Visit http://localhost:3000/service-areas — verify interactive map shows with colored city polygons. Hover over a city to see its name. Click a city to confirm it navigates to /locations/{slug}.
  2. Visit http://localhost:3000/get-quote — verify first step is "Address" with an autocomplete input. Type a Madison address (e.g., "123 East Washington") and verify suggestions appear. Select one and confirm city/zip auto-fill. Proceed through all steps to confirm existing flow still works.
  3. Visit http://localhost:3000/annual-plan — verify services load, toggle a few services across seasons, confirm calendar updates, confirm pricing recalculates. Select 3+ services and verify 15% bundle discount appears. Click "Lock In My Plan" and verify contact form appears.
  4. Check the homepage for a link/CTA pointing to the annual plan page.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- TypeScript: `npx tsc --noEmit 2>&1 | grep -c "error TS"` = 84 or fewer
- Build: `npm run build` passes
- Service area map: 12 cities visible, clickable, tooltips work
- Quote flow: address autocomplete works, all 7 steps functional, lead submission works
- Annual plan: service toggles update calendar and pricing, bundle discount at 3+ services, lead capture works
- Homepage: CTA to annual plan visible
</verification>

<success_criteria>
- All 18 requirements (QUOTE-01 through QUOTE-07, MAP-01 through MAP-05, PLAN-01 through PLAN-06) verified
- TypeScript error count at or below 84 (zero new errors)
- Build succeeds
- Visual QA approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/07-conversion-features/07-05-SUMMARY.md`
</output>
