---
phase: 07-conversion-features
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/ServiceAreaMap.tsx
  - src/app/service-areas/ServiceAreasContent.tsx
autonomous: true

must_haves:
  truths:
    - "Interactive map displays all 12 Dane County service cities with colored boundary overlays"
    - "Clicking a city polygon navigates to /locations/{slug}"
    - "City name appears on hover/tooltip"
    - "Map replaces the existing Google Maps iframe on the service-areas page"
    - "Map renders correctly without SSR crashes (next/dynamic ssr:false)"
  artifacts:
    - path: "src/components/ServiceAreaMap.tsx"
      provides: "Leaflet interactive map with city polygons"
      min_lines: 50
    - path: "src/app/service-areas/ServiceAreasContent.tsx"
      provides: "Service areas page with embedded interactive map"
      contains: "ServiceAreaMap"
  key_links:
    - from: "src/components/ServiceAreaMap.tsx"
      to: "src/data/daneCountyBoundaries.ts"
      via: "import CITY_BOUNDARIES"
      pattern: "CITY_BOUNDARIES"
    - from: "src/components/ServiceAreaMap.tsx"
      to: "/locations/{slug}"
      via: "router.push or window.location on polygon click"
      pattern: "locations/"
    - from: "src/app/service-areas/ServiceAreasContent.tsx"
      to: "src/components/ServiceAreaMap.tsx"
      via: "next/dynamic with ssr:false"
      pattern: "dynamic.*ServiceAreaMap"
---

<objective>
Build the interactive Leaflet service area map component and integrate it into the service-areas page, replacing the current Google Maps iframe.

Purpose: MAP-01 through MAP-05 requirements. Visitors see TotalGuard's service area as an interactive map with clickable city polygons that navigate to location pages.

Output: New ServiceAreaMap.tsx component, updated ServiceAreasContent.tsx with map integration.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-conversion-features/07-RESEARCH.md
@src/app/service-areas/ServiceAreasContent.tsx
@src/data/daneCountyBoundaries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ServiceAreaMap Leaflet component</name>
  <files>src/components/ServiceAreaMap.tsx</files>
  <action>
  Create `src/components/ServiceAreaMap.tsx` as a client component:

  1. Add `'use client'` directive
  2. Import from react-leaflet: `MapContainer`, `TileLayer`, `Polygon`, `Tooltip`
  3. Import `'leaflet/dist/leaflet.css'` directly in this file (CRITICAL — Leaflet CSS must be imported in the client component, not globally)
  4. Import `CITY_BOUNDARIES` from `@/data/daneCountyBoundaries`
  5. Import `useRouter` from `next/navigation` for client-side navigation

  Component structure:
  - Props: `className?: string`, `height?: string` (default '500px')
  - MapContainer centered on Madison [43.073, -89.401], zoom 10
  - Dark-themed tile layer: use CartoDB dark_all tiles `https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png` to match TotalGuard's dark theme
  - Attribution for both CartoDB and OpenStreetMap
  - For each city in CITY_BOUNDARIES, render a `<Polygon>`:
    - `positions={city.coordinates}` (already in [lat,lng] format from data file)
    - `pathOptions={{ color: city.color, fillColor: city.color, fillOpacity: 0.25, weight: 2 }}`
    - `eventHandlers={{ click: () => router.push('/locations/' + city.slug) }}`
    - Hover effect: on mouseover, increase fillOpacity to 0.45; on mouseout, reset to 0.25. Use `eventHandlers` with `mouseover` and `mouseout` that call `e.target.setStyle()`
    - Inside each Polygon, add `<Tooltip sticky>{city.name}</Tooltip>` (sticky follows cursor, NOT permanent — permanent would clutter the map)

  Style the container:
  - Wrap MapContainer in a div with `rounded-xl overflow-hidden border border-white/10`
  - Add a subtle glow shadow matching the brand green

  IMPORTANT: Do NOT use default Leaflet markers (blue teardrop) — they 404 in Next.js. We only use Polygon + Tooltip.
  IMPORTANT: TypeScript error budget is 84/85. Cast polygon positions as `LatLngExpression[][]` if needed to avoid type errors. Add ZERO new TS errors.
  </action>
  <verify>
  - `npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 84 or less
  - `grep "MapContainer" src/components/ServiceAreaMap.tsx` confirms Leaflet usage
  - `grep "leaflet/dist/leaflet.css" src/components/ServiceAreaMap.tsx` confirms CSS import
  - `grep "CITY_BOUNDARIES" src/components/ServiceAreaMap.tsx` confirms data import
  - `grep "locations/" src/components/ServiceAreaMap.tsx` confirms click navigation
  </verify>
  <done>ServiceAreaMap.tsx renders a dark-themed Leaflet map with 12 colored city polygons, hover highlight effects, sticky tooltips showing city names, and click-to-navigate to /locations/{slug}.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate map into ServiceAreasContent page</name>
  <files>src/app/service-areas/ServiceAreasContent.tsx</files>
  <action>
  Modify `src/app/service-areas/ServiceAreasContent.tsx` to replace the Google Maps iframe section with the interactive Leaflet map:

  1. Add `import dynamic from 'next/dynamic'` at top of file
  2. Add dynamic import OUTSIDE the component (module level):
     ```typescript
     const ServiceAreaMap = dynamic(
       () => import('@/components/ServiceAreaMap').then(m => m.ServiceAreaMap),
       {
         ssr: false,
         loading: () => (
           <div style={{ height: '500px' }} className="animate-pulse bg-white/5 rounded-xl" />
         ),
       }
     );
     ```
  3. Find the section that contains the Google Maps iframe (look for `<iframe` or a map placeholder section). Replace it with `<ServiceAreaMap />`.
  4. If there's no iframe section, add the map as a new prominent section after the hero and before the city grid. Give it a heading like "Explore Our Service Area" with a subheading "Click any city to learn more about our services there."
  5. Keep ALL existing content (city grid, services list, schemas, etc.) intact. Only ADD the map or REPLACE the iframe.

  Style integration:
  - Section container: `py-16` or `py-20` for spacing
  - Center the heading above the map
  - Map width: full container width with `max-w-5xl mx-auto px-4`

  IMPORTANT: Do NOT remove the ItemListSchema, WebPageSchema, or any existing SEO content. Only the iframe (if present) gets replaced.
  IMPORTANT: TypeScript error budget is 84/85. Add ZERO new TS errors.
  </action>
  <verify>
  - `npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 84 or less
  - `grep "ServiceAreaMap" src/app/service-areas/ServiceAreasContent.tsx` confirms map integration
  - `grep "ssr: false" src/app/service-areas/ServiceAreasContent.tsx` confirms SSR disabled for map
  - `grep "ItemListSchema" src/app/service-areas/ServiceAreasContent.tsx` confirms schemas preserved
  - `npm run build 2>&1 | tail -5` — build succeeds (no SSR crash from Leaflet)
  </verify>
  <done>ServiceAreasContent.tsx displays the interactive Leaflet map via next/dynamic (ssr:false) with loading skeleton. All existing SEO schemas and content preserved. Google Maps iframe removed/replaced.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors (Leaflet does not crash SSR)
- `npx tsc --noEmit 2>&1 | grep -c "error TS"` — still 84 or fewer errors
- Service areas page renders with interactive map showing 12 colored city polygons
- Clicking a city polygon navigates to /locations/{slug}
- Hovering shows city name tooltip
- Map uses dark theme tiles matching TotalGuard brand
</verification>

<success_criteria>
- MAP-01: Interactive Leaflet + OpenStreetMap map renders on service-areas page
- MAP-02: 12 Dane County cities shown with colored boundary overlays
- MAP-03: Clicking a city navigates to /locations/{slug}
- MAP-04: City name shown on hover via sticky Tooltip
- MAP-05: Replaces Google Maps iframe
- TypeScript error count unchanged at 84
</success_criteria>

<output>
After completion, create `.planning/phases/07-conversion-features/07-02-SUMMARY.md`
</output>
