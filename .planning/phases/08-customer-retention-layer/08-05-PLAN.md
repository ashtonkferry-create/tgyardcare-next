---
wave: 4
depends_on: [08-02, 08-03, 08-04]
files_modified: []
autonomous: true
---

# Plan 08-05: Phase 8 Verification

## Goal
Verify all Phase 8 work: build passes, TypeScript clean, all portal routes render, auth flow works, referral page works.

## Verification Steps

1. Run `npx tsc --noEmit` — must exit 0 (or same error count as baseline 84)
2. Run `npm run build` — must complete with all portal routes in output
3. Check portal routes in build output:
   - /portal/login
   - /portal/dashboard
   - /portal/auth/callback
   - /r/[code]
4. Verify middleware protects /portal/dashboard → /portal/login
5. Verify /portal/login and /portal/auth/callback are NOT protected
6. Verify migration file exists and is syntactically valid
7. Verify ReferralCard copy-to-clipboard function exists
8. Verify /r/[code] is a server component (no 'use client' at top)

## Commit Summary
After verification, create commit:
```
feat(phase-8): customer portal — magic link auth, dashboard, referral engine
```

## must_haves
- [ ] tsc --noEmit exits 0 or same baseline
- [ ] npm run build succeeds
- [ ] /portal/login in build output
- [ ] /portal/dashboard in build output
- [ ] /r/[code] in build output
- [ ] Migration file syntactically valid
