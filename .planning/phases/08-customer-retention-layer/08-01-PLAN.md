---
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260318_phase8_portal_tables.sql
autonomous: true
---

# Plan 08-01: Portal Database Schema

## Goal
Create all Supabase tables needed for the customer portal and referral engine with proper RLS policies.

## Tables to Create

### portal_customers
Links Supabase auth user to portal profile.
```sql
CREATE TABLE portal_customers (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE UNIQUE,
  name text NOT NULL,
  email text NOT NULL,
  phone text,
  address text,
  referral_code text UNIQUE NOT NULL,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
```

### service_records
Completed past services.
```sql
CREATE TABLE service_records (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  customer_id uuid REFERENCES portal_customers(id) ON DELETE CASCADE,
  service_date date NOT NULL,
  service_type text NOT NULL,
  description text,
  crew_notes text,
  amount_cents integer NOT NULL DEFAULT 0,
  created_at timestamptz DEFAULT now()
);
```

### upcoming_jobs
Scheduled future services.
```sql
CREATE TABLE upcoming_jobs (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  customer_id uuid REFERENCES portal_customers(id) ON DELETE CASCADE,
  scheduled_date date NOT NULL,
  service_type text NOT NULL,
  description text,
  time_window text DEFAULT 'Morning (8am-12pm)',
  status text DEFAULT 'scheduled' CHECK (status IN ('scheduled','confirmed','en_route','completed','cancelled')),
  created_at timestamptz DEFAULT now()
);
```

### customer_invoices
Invoice records with payment status.
```sql
CREATE TABLE customer_invoices (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  customer_id uuid REFERENCES portal_customers(id) ON DELETE CASCADE,
  invoice_date date NOT NULL,
  due_date date,
  amount_cents integer NOT NULL,
  status text DEFAULT 'pending' CHECK (status IN ('pending','paid','overdue')),
  description text,
  invoice_number text,
  created_at timestamptz DEFAULT now()
);
```

### service_ratings
Customer crew ratings.
```sql
CREATE TABLE service_ratings (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  customer_id uuid REFERENCES portal_customers(id) ON DELETE CASCADE,
  service_record_id uuid REFERENCES service_records(id) ON DELETE CASCADE,
  rating integer NOT NULL CHECK (rating >= 1 AND rating <= 5),
  comment text,
  created_at timestamptz DEFAULT now(),
  UNIQUE(service_record_id)
);
```

### referral_events
Tracks referral outcomes.
```sql
CREATE TABLE referral_events (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  referral_code text NOT NULL REFERENCES portal_customers(referral_code),
  referred_email text NOT NULL,
  referred_name text,
  status text DEFAULT 'pending' CHECK (status IN ('pending','qualified','credited')),
  referrer_credit_cents integer DEFAULT 5000,
  referred_discount_cents integer DEFAULT 5000,
  created_at timestamptz DEFAULT now(),
  qualified_at timestamptz,
  credited_at timestamptz
);
```

### service_requests
Additional service requests from portal.
```sql
CREATE TABLE service_requests (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  customer_id uuid REFERENCES portal_customers(id) ON DELETE CASCADE,
  service_type text NOT NULL,
  preferred_date date,
  notes text,
  status text DEFAULT 'pending' CHECK (status IN ('pending','reviewing','scheduled','declined')),
  created_at timestamptz DEFAULT now()
);
```

## RLS Policies
All tables: customers can only see their own rows.
Admin (service_role) can see all.

## must_haves
- [ ] Migration file created and applies cleanly (no SQL errors)
- [ ] RLS enabled on all 7 tables
- [ ] Customers can only SELECT/INSERT/UPDATE their own rows
- [ ] Migration applied to Supabase production
