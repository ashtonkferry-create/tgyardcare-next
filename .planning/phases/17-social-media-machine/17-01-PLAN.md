---
phase: 17-social-media-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - automation/n8n-workflows/TG-133-weekly-content-batch.json
  - automation/SOCIAL-CONTENT-CALENDAR.md
autonomous: true

must_haves:
  truths:
    - "Weekly batch of 7 AI-generated social posts exists in Supabase every Monday"
    - "Each post has platform-specific captions, hashtags, and pillar category"
    - "Posts follow a 7-day pillar calendar (tips, before/after, reviews, seasonal, community, education, promo)"
  artifacts:
    - path: "automation/n8n-workflows/TG-133-weekly-content-batch.json"
      provides: "Weekly content generation workflow"
      contains: "TG-133"
    - path: "automation/SOCIAL-CONTENT-CALENDAR.md"
      provides: "Pillar calendar documentation and prompt templates"
      contains: "pillar"
  key_links:
    - from: "TG-133"
      to: "Supabase social_posts table"
      via: "HTTP Request to Supabase REST API"
      pattern: "social_posts"
    - from: "TG-133"
      to: "Claude API"
      via: "HTTP Request for AI content generation"
      pattern: "anthropic"

user_setup:
  - service: supabase
    why: "Social posts storage table"
    dashboard_config:
      - task: "Create social_posts table with SQL from plan"
        location: "Supabase Dashboard > SQL Editor"
---

<objective>
Create the weekly AI content generation engine that produces 7 social media posts every Monday, following a pillar calendar, and stores them in Supabase for daily publishing.

Purpose: SOCL-01 (weekly content batch) and SOCL-02 (AI-generated content with pillar calendar) -- the content supply that feeds the daily posting pipeline.
Output: TG-133 n8n workflow JSON + pillar calendar documentation + Supabase table SQL.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-foundation-infrastructure/11-03-SUMMARY.md
@automation/AUTOPOST-SETUP.md
@automation/n8n-workflows/TG-AUTOPOST-social-publisher.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create social_posts Supabase table SQL and pillar calendar docs</name>
  <files>automation/SOCIAL-CONTENT-CALENDAR.md</files>
  <action>
Create automation/SOCIAL-CONTENT-CALENDAR.md with two sections:

**Section 1: Supabase Table SQL**

```sql
CREATE TABLE IF NOT EXISTS social_posts (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  post_text text NOT NULL,
  image_url text,
  platforms text[] DEFAULT ARRAY['instagram', 'linkedin', 'pinterest'],
  post_type text NOT NULL,
  pillar text NOT NULL,
  day_of_week integer NOT NULL CHECK (day_of_week BETWEEN 0 AND 6),
  scheduled_for timestamptz,
  published_at timestamptz,
  status text DEFAULT 'draft' CHECK (status IN ('draft', 'scheduled', 'published', 'failed')),
  batch_week date NOT NULL,
  engagement_data jsonb DEFAULT '{}',
  source text DEFAULT 'ai_generated' CHECK (source IN ('ai_generated', 'review_highlight', 'manual')),
  review_id uuid,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);

CREATE INDEX idx_social_posts_status ON social_posts(status);
CREATE INDEX idx_social_posts_scheduled ON social_posts(scheduled_for);
CREATE INDEX idx_social_posts_batch ON social_posts(batch_week);
```

**Section 2: 7-Day Pillar Calendar**

| Day | Pillar | Content Type | Example |
|-----|--------|-------------|---------|
| Monday | Tips & Tricks | Quick lawn care tip | "3 signs your lawn needs aeration" |
| Tuesday | Before/After | Transformation showcase | Photo pair with stats |
| Wednesday | Review Spotlight | 5-star review highlight | Quote card from Google review |
| Thursday | Seasonal | Timely seasonal advice | Spring prep, fall cleanup tips |
| Friday | Community | Madison local content | Local events, neighborhood shoutouts |
| Saturday | Education | Deep dive lawn science | Why fertilizer timing matters |
| Sunday | Promo/CTA | Service promotion | Special offer or quote CTA |

**Section 3: AI Prompt Templates**

Include Claude Haiku prompt templates for each pillar type. Each prompt should:
- Reference TotalGuard brand voice (friendly, knowledgeable, local Madison WI)
- Include hashtag generation (5-8 per post, mix of lawn care + Madison local)
- Specify character limits per platform (Instagram 2200, LinkedIn 3000, TikTok 150, Pinterest 500)
- Include seasonal awareness (current month/season context)
- Generate platform-specific variations when needed

**Section 4: Integration Notes**
- Table must be created manually by Vance in Supabase SQL Editor
- TG-133 writes to this table, TG-135 reads from it for daily publishing
- Wednesday posts auto-filled by TG-134 (review highlights) when available
  </action>
  <verify>File exists at automation/SOCIAL-CONTENT-CALENDAR.md with all 4 sections, SQL is valid syntax, all 7 pillars defined.</verify>
  <done>Pillar calendar documented, SQL ready for Vance to run, prompt templates cover all 7 content types.</done>
</task>

<task type="auto">
  <name>Task 2: Build TG-133 weekly content batch generation workflow</name>
  <files>automation/n8n-workflows/TG-133-weekly-content-batch.json</files>
  <action>
Create a proper n8n workflow JSON file for TG-133 with these nodes:

1. **Schedule Trigger** -- Fires every Monday at 6:00 AM CT (America/Chicago). Use cron: `0 6 * * 1`.

2. **Get Current Context** (Code node) -- Builds context object:
   - current_date, current_month, current_season (derived from month)
   - batch_week (Monday's date as ISO string)
   - services array (from PROJECT.md: mowing, fertilization, aeration, etc.)
   - Madison WI location context
   - Pillar assignments for each day (Mon=tips, Tue=before_after, Wed=review_spotlight, Thu=seasonal, Fri=community, Sat=education, Sun=promo)

3. **Check Existing Batch** (HTTP Request) -- GET to Supabase REST API checking if social_posts already exist for this batch_week. URL: `https://lwtmvzhwekgdxkaisfra.supabase.co/rest/v1/social_posts?batch_week=eq.{{batch_week}}&select=id`. Headers: apikey + Authorization with anon key `sb_publishable_DeX21ldoDKl3NyJeFZzR1w_eWBUkx-v`. If posts exist, stop (prevent duplicates).

4. **IF No Existing Posts** -- Only continue if Step 3 returns empty array.

5. **Generate 7 Posts** (HTTP Request to Claude API) -- POST to `https://api.anthropic.com/v1/messages` with model `claude-3-5-haiku-20241022`. System prompt instructs Claude to generate 7 social media posts as JSON array, one per day, following the pillar calendar. Each post object: `{day_of_week, pillar, post_text, hashtags, post_type, platforms}`. Use placeholder `ANTHROPIC_API_KEY` for the API key header. Max tokens: 4000.

6. **Parse AI Response** (Code node) -- Extracts the JSON array from Claude's response. Validates 7 posts exist. Adds batch_week and scheduled_for timestamps (each day at 10:00 AM CT). Sets status to "scheduled". Skips Wednesday if post_type is "review_spotlight" (TG-134 fills those).

7. **Insert Posts to Supabase** (HTTP Request) -- POST to `https://lwtmvzhwekgdxkaisfra.supabase.co/rest/v1/social_posts` with the 7 post objects. Headers: apikey + Authorization with anon key, Content-Type: application/json, Prefer: return=representation.

8. **Send Telegram Confirmation** (HTTP Request) -- POST to Telegram Bot API notifying Vance that 7 posts were generated for the week. Use placeholder `OWNER_TELEGRAM_CHAT_ID` and `TELEGRAM_BOT_TOKEN`. Message format: "Social Media Batch Ready -- 7 posts generated for week of {date}. Review in Supabase social_posts table."

Use these n8n workflow conventions:
- Workflow name: "TG-133 Weekly Content Batch"
- Tags: use tag ID `fPXl9eiMhJc3ISQQ` (TG- tag)
- All nodes positioned with reasonable x/y coordinates (200px spacing)
- Connections array linking nodes sequentially
- Use `n8n-nodes-base.scheduleTrigger` for the cron trigger
- Use `n8n-nodes-base.httpRequest` for API calls
- Use `n8n-nodes-base.code` for JavaScript logic
- Use `n8n-nodes-base.if` for conditional

IMPORTANT: Use placeholder strings for secrets:
- `ANTHROPIC_API_KEY` for Claude API key
- `OWNER_TELEGRAM_CHAT_ID` for Telegram chat ID
- `TELEGRAM_BOT_TOKEN` for Telegram bot token
- Supabase anon key is publishable and can be hardcoded: `sb_publishable_DeX21ldoDKl3NyJeFZzR1w_eWBUkx-v`
  </action>
  <verify>JSON is valid (parse with jq or node -e). Workflow has 8 nodes. Schedule trigger set to Monday 6 AM. Claude API call uses haiku model. Supabase URLs correct. Facebook not in any platforms array.</verify>
  <done>TG-133 workflow JSON ready for import into n8n. Generates 7 pillar-based posts weekly, stores in Supabase, notifies via Telegram.</done>
</task>

</tasks>

<verification>
- automation/SOCIAL-CONTENT-CALENDAR.md exists with SQL, pillar calendar, prompts, integration notes
- automation/n8n-workflows/TG-133-weekly-content-batch.json is valid JSON with 8 nodes
- No Facebook references in any platforms array
- No real API keys committed (only publishable Supabase anon key)
- Supabase URL matches project: lwtmvzhwekgdxkaisfra.supabase.co
</verification>

<success_criteria>
- Weekly content batch workflow ready for n8n import
- Pillar calendar covers all 7 days with distinct content types
- AI prompt templates produce TotalGuard-branded, Madison-localized content
- Supabase table SQL ready for manual creation
</success_criteria>

<output>
After completion, create `.planning/phases/17-social-media-machine/17-01-SUMMARY.md`
</output>
