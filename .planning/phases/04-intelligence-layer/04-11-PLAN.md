---
phase: 04-intelligence-layer
plan: 11
type: execute
wave: 5
depends_on: ["04-02", "04-04"]
files_modified:
  - automation/n8n-workflows/TG-83-quote-followup-sequence.json
  - automation/n8n-workflows/TG-84-invoice-collections-sequence.json
  - automation/n8n-workflows/TG-85-missed-call-capture.json
  - automation/n8n-workflows/TG-91-abandoned-quote-sms.json
  - automation/n8n-workflows/TG-94-unified-sms-sender.json
  - automation/n8n-workflows/TG-95-unified-email-sender.json
autonomous: true

must_haves:
  truths:
    - "TG-83, TG-84, TG-85, TG-91 each call TG-105 (A/B test router) via executeWorkflow before sending SMS/email"
    - "If TG-105 returns is_default=true, the workflow uses its original hardcoded message (no change in behavior)"
    - "If TG-105 returns variant_content, the workflow uses that content instead of the default"
    - "TG-94 and TG-95 accept optional ab_test_id and ab_variant_id in their input and log them to sms_sends/email_sends"
    - "All modifications are backward-compatible — workflows function identically if no A/B test is active"
  artifacts:
    - path: "automation/n8n-workflows/TG-83-quote-followup-sequence.json"
      provides: "Quote follow-up with A/B test integration"
      contains: "executeWorkflow"
    - path: "automation/n8n-workflows/TG-84-invoice-collections-sequence.json"
      provides: "Invoice collections with A/B test integration"
      contains: "executeWorkflow"
    - path: "automation/n8n-workflows/TG-85-missed-call-capture.json"
      provides: "Missed call capture with A/B test integration"
      contains: "executeWorkflow"
    - path: "automation/n8n-workflows/TG-91-abandoned-quote-sms.json"
      provides: "Abandoned quote SMS with A/B test integration"
      contains: "executeWorkflow"
    - path: "automation/n8n-workflows/TG-94-unified-sms-sender.json"
      provides: "Unified SMS sender with A/B logging"
      contains: "ab_test_id"
    - path: "automation/n8n-workflows/TG-95-unified-email-sender.json"
      provides: "Unified email sender with A/B logging"
      contains: "ab_test_id"
  key_links:
    - from: "TG-83/84/85/91"
      to: "TG-105"
      via: "executeWorkflow before each send"
      pattern: "executeWorkflow"
    - from: "TG-94"
      to: "sms_sends"
      via: "INSERT with ab_test_id, ab_variant_id columns"
      pattern: "ab_test_id"
    - from: "TG-95"
      to: "email_sends"
      via: "INSERT with ab_test_id, ab_variant_id columns"
      pattern: "ab_test_id"
---

<objective>
Modify 6 existing workflows to integrate A/B testing. TG-83/84/85/91 get a TG-105 call before sending. TG-94/95 get ab_test_id + ab_variant_id columns in their send logging.

Purpose: This connects the A/B test router (TG-105) to actual message sending, enabling the self-improvement loop. Without these modifications, the A/B infrastructure has no data flowing through it.
Output: 6 updated workflow JSON files + deployed updates to n8n.
</objective>

<execution_context>
@C:\Users\vance\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\vance\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-intelligence-layer/04-RESEARCH.md
@.planning/phases/04-intelligence-layer/04-02-SUMMARY.md
@automation/n8n-workflows/TG-83-quote-followup-sequence.json
@automation/n8n-workflows/TG-84-invoice-collections-sequence.json
@automation/n8n-workflows/TG-85-missed-call-capture.json
@automation/n8n-workflows/TG-91-abandoned-quote-sms.json
@automation/n8n-workflows/TG-94-unified-sms-sender.json
@automation/n8n-workflows/TG-95-unified-email-sender.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify TG-83, TG-84, TG-85, TG-91 to call TG-105 A/B Test Router</name>
  <files>automation/n8n-workflows/TG-83-quote-followup-sequence.json, automation/n8n-workflows/TG-84-invoice-collections-sequence.json, automation/n8n-workflows/TG-85-missed-call-capture.json, automation/n8n-workflows/TG-91-abandoned-quote-sms.json</files>
  <action>
For each of the 4 sending workflows, insert a TG-105 call before each SMS/email send point.

**Modification pattern (same for all 4 workflows):**

Before each point where the workflow sends an SMS (via TG-94) or email (via TG-95), insert:

1. **Execute Sub-workflow "Get A/B Variant" node**
   - workflowId: {{ TG-105 ID from 04-02 }}
   - Input: {
       test_id: '{{ workflow_name }}-{{ send_point }}-sms',  // e.g., 'quote-followup-day2-sms'
       channel: 'sms' (or 'email'),
       recipient_phone: {{ customer phone }},
       recipient_email: {{ customer email }}
     }

2. **IF "Use Variant?" node**
   - condition: {{ $json.is_default === false }}
   - true: use variant_content as message body
   - false: use original hardcoded message (no change)

3. **Merge node** (combines variant content with existing data flow)
   - Pass ab_test_id and ab_variant_id to the TG-94/TG-95 call

**Specific send points per workflow:**

TG-83 (Quote Follow-up):
- Day 2 SMS → test_id: 'tg83-day2-sms'
- Day 7 Email → test_id: 'tg83-day7-email'

TG-84 (Invoice Collections):
- Day 3 SMS → test_id: 'tg84-day3-sms'
- Day 10 Email → test_id: 'tg84-day10-email'

TG-85 (Missed Call):
- Auto-reply SMS → test_id: 'tg85-autoreply-sms'

TG-91 (Abandoned Quote):
- Re-engagement SMS → test_id: 'tg91-reengagement-sms'

**Add to TG-94/TG-95 calls:**
Include ab_test_id and ab_variant_id in the input to the executeWorkflow calls for TG-94 and TG-95:
```json
{
  "to_phone": "...",
  "message_body": "{{ variant_content || default_message }}",
  "workflow_name": "TG-83",
  "message_type": "followup",
  "ab_test_id": "{{ ab_test_id || null }}",
  "ab_variant_id": "{{ ab_variant_id || null }}"
}
```

CRITICAL NOTES:
1. Read each existing workflow JSON FIRST to understand its current node structure
2. Insert new nodes WITHOUT disrupting existing connections
3. The IF "Use Variant?" ensures backward compatibility — if no test is active, original behavior is preserved
4. Each modification adds 2-3 nodes per send point
5. TG-83 and TG-84 have 2 send points each (SMS + email), TG-85 and TG-91 have 1 each
6. Total: 8 new node insertions across 4 workflows
7. executeWorkflowTrigger typeVersion 1 for TG-105 calls
8. n8n rate limit: updating 4 workflows = ~4-8 API calls

Update local JSONs. Deploy updates to n8n.
  </action>
  <verify>
1. TG-83 has executeWorkflow calls to TG-105 before Day 2 SMS and Day 7 email
2. TG-84 has executeWorkflow calls before Day 3 SMS and Day 10 email
3. TG-85 has executeWorkflow call before auto-reply SMS
4. TG-91 has executeWorkflow call before re-engagement SMS
5. All pass ab_test_id + ab_variant_id to TG-94/TG-95 calls
6. All default to original message when is_default=true
7. Updated JSONs saved locally
8. Updated workflows deployed to n8n
  </verify>
  <done>4 sending workflows modified with A/B test integration. 8 test points across TG-83/84/85/91. Backward-compatible — original behavior preserved when no test is active.</done>
</task>

<task type="auto">
  <name>Task 2: Modify TG-94 + TG-95 to log ab_test_id and ab_variant_id</name>
  <files>automation/n8n-workflows/TG-94-unified-sms-sender.json, automation/n8n-workflows/TG-95-unified-email-sender.json</files>
  <action>
Modify both unified senders to accept and log A/B test data.

**TG-94 (Unified SMS Sender):**
1. Update the executeWorkflowTrigger input schema to accept optional: ab_test_id, ab_variant_id
2. In the "Log to sms_sends" HTTP Request node, add the two new columns to the INSERT body:
   ```json
   {
     "to_phone": "...",
     "message_body": "...",
     "workflow_name": "...",
     "ab_test_id": "{{ $json.ab_test_id || null }}",
     "ab_variant_id": "{{ $json.ab_variant_id || null }}"
   }
   ```
3. No other changes needed — the columns were added in migration 072

**TG-95 (Unified Email Sender):**
1. Same pattern — accept optional ab_test_id, ab_variant_id
2. In the "Log to email_sends" node, add:
   ```json
   {
     "to_email": "...",
     "subject": "...",
     "ab_test_id": "{{ $json.ab_test_id || null }}",
     "ab_variant_id": "{{ $json.ab_variant_id || null }}"
   }
   ```

CRITICAL NOTES:
1. These are MINOR modifications — just adding 2 fields to existing INSERT bodies
2. The fields are nullable — passing null when no A/B test is active is correct
3. Read existing JSON first to find the exact node that does the INSERT
4. Do NOT change any other behavior of TG-94/TG-95
5. n8n rate limit: 2 workflow updates = ~2-4 API calls

Update local JSONs. Deploy updates to n8n.
  </action>
  <verify>
1. TG-94 accepts ab_test_id, ab_variant_id in input
2. TG-94 logs ab_test_id, ab_variant_id to sms_sends table
3. TG-95 accepts ab_test_id, ab_variant_id in input
4. TG-95 logs ab_test_id, ab_variant_id to email_sends table
5. Both pass null when no A/B data provided (backward compatible)
6. Updated JSONs saved locally
7. Both workflows re-deployed to n8n and ACTIVE
  </verify>
  <done>TG-94 and TG-95 updated with A/B test logging. Every SMS and email now records which A/B variant was used (if any). Full A/B data pipeline is connected.</done>
</task>

</tasks>

<verification>
- A/B test flow: TG-83 calls TG-105 -> gets variant -> passes ab_test_id to TG-94 -> TG-94 logs to sms_sends
- All 6 modified workflows backward-compatible (no behavior change when no test active)
- sms_sends and email_sends now populated with ab_test_id when A/B tests are active
- TG-106 (auto-winner) can now read send data for winner declaration
</verification>

<success_criteria>
The A/B testing loop is fully connected: TG-105 selects variants, sending workflows use them, TG-94/95 log them, TG-106 reads logs to declare winners. The entire A/B testing system is operational end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/04-intelligence-layer/04-11-SUMMARY.md`

Record:
- All 6 workflows updated on n8n (list IDs)
- Number of A/B test points per workflow
- n8n API calls used (track against rate limit)
- End-to-end A/B data flow confirmed
</output>
