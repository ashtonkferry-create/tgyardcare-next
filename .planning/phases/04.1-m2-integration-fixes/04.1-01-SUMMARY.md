---
phase: 04.1-m2-integration-fixes
plan: 01
completed: 2026-03-16
---

# Phase 4.1 Plan 1: M2 Integration Fixes Summary

**Fixed all 7 cross-phase integration issues from M2-MILESTONE-AUDIT.md**

## Fixes Applied

| Issue | File | Fix | Verified |
|-------|------|-----|----------|
| W1 | TG-76-two-way-sms.json | `TG94_WORKFLOW_ID` → `AprqI2DgQA8lehij` | grep: 1 match, 0 placeholders |
| W2 | TG-05-jobber-email-parser.json | Empty workflowIds → `9m2sID72Fz1PF0HY` (TG-83), `63t7K6gAdW1aPupP` (TG-84), `wJvqFV6PyGvUZccX` (TG-86) | grep: 3 IDs present |
| S1 | TG-05-jobber-email-parser.json | `event_type`→`parsed_event_type`, `customer_name`→`parsed_customer_name`, `customer_email`→`parsed_customer_email`, `customer_phone`→`parsed_customer_phone`, `address`→`parsed_address`, `service`→`parsed_service`, `amount`→`parsed_amount`, `email_subject`→`raw_email_subject`, `received_at`→`parsed_at`, removed `jobber_id` (no column) | Verified against live schema |
| S2 | TG-92-webhook-router.json | `status`→`consent_status` in opt-out upsert, opt-in upsert, and consent check query | grep: 3 matches |
| S3 | TG-94-unified-sms-sender.json | `phone_number`→`p_phone` in can_send_sms RPC call | grep: 1 match, 0 old param |
| S4 | Migration 073 | `ALTER TABLE ab_test_variants ADD COLUMN content TEXT` | Applied to Supabase |
| D2 | TG-95-unified-email-sender.json | Added 2 logging nodes (post-send + post-retry) writing to email_sends | grep: 6 references |

## Schema Verification Method

All column/parameter names verified against **live Supabase** via `information_schema.columns` and `pg_proc` queries before making any changes. No guessing.

## Commit

`5a5e3ca` — fix(m2): resolve 7 cross-phase integration issues from milestone audit

## JSON Validation

All 5 modified JSON files validated with `JSON.parse()` — zero syntax errors.
