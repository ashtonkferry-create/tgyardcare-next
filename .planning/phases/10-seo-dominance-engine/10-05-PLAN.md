---
wave: 5
depends_on: [10-04]
files_modified: []
autonomous: true
---

# Plan 10-05: Phase 10 + Full M3 Verification

## Goal
Full final verification pass covering Phase 10 specifically, then the entire M3 milestone (Phases 5-10).

## Phase 10 Verification
1. `npx tsc --noEmit` — exit 0 or same baseline
2. `npm run build` — must complete
3. Build output includes ≥96 city-service pages under [cityService]
4. /lawn-care-costs-dane-county in build
5. /seasonal-lawn-calendar-madison in build
6. /lawn-care-guide in build
7. Breadcrumb appears on at least the city-service page
8. Service pages have "Serving Across Dane County" section (grep check)
9. Sitemap includes 96+ city-service URLs

## Full M3 Milestone Verification
Grep-based checks:
- `grep -r "portal_customers" src/` → finds the dashboard page
- `grep -r "signInWithOtp" src/` → finds portal login
- `grep -r "referral_code" src/` → finds referral card
- `grep -r "lawn-care-guide" src/` → finds links to hub
- `grep -r "getCityServiceParams" src/` → finds dynamic route
- `grep -r "Serving Across Dane County" src/` → finds all 8 service pages
- `grep -r "LeadMagnetCTA" src/` → finds blog post integration

## Final Commits
```
feat(phase-10): 96 city-service pages, linkable assets, breadcrumbs, internal link web
docs(m3): update ROADMAP.md — Phases 8, 9, 10 complete
```

Update .planning/ROADMAP.md progress table to mark 8, 9, 10 complete.
Update .planning/STATE.md to reflect M3 milestone complete.

## must_haves
- [ ] tsc passes
- [ ] build passes with 96+ city-service pages
- [ ] All M3 grep checks pass (7 checks above)
- [ ] ROADMAP.md updated
- [ ] STATE.md updated
