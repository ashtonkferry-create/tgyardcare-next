{
  "name": "TG-97-rank-drop-detector",
  "nodes": [
    {
      "parameters": {},
      "id": "trigger",
      "name": "Execute Workflow Trigger",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1,
      "position": [0, 300]
    },
    {
      "parameters": {
        "jsCode": "// Calculate date range: current syncDate vs 7 days prior\nconst syncDate = $json.syncDate;\nconst prevDate = new Date(new Date(syncDate).getTime() - 7 * 24 * 60 * 60 * 1000).toISOString().split('T')[0];\nreturn [{ json: { currentDate: syncDate, previousDate: prevDate } }];"
      },
      "id": "calc-range",
      "name": "Calculate Date Range",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [220, 300]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "=https://lwtmvzhwekgdxkaisfra.supabase.co/rest/v1/gsc_search_queries?date=eq.{{ $json.currentDate }}&impressions=gt.0&select=query,page,clicks,impressions,ctr,position",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "SUPABASE_SECRET_KEY_PLACEHOLDER"
            },
            {
              "name": "Authorization",
              "value": "Bearer SUPABASE_SECRET_KEY_PLACEHOLDER"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "fullResponse": true
            }
          }
        }
      },
      "id": "fetch-current",
      "name": "Fetch Current Queries",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [440, 200]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "=https://lwtmvzhwekgdxkaisfra.supabase.co/rest/v1/gsc_search_queries?date=eq.{{ $json.previousDate }}&impressions=gt.0&select=query,page,clicks,impressions,ctr,position",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "SUPABASE_SECRET_KEY_PLACEHOLDER"
            },
            {
              "name": "Authorization",
              "value": "Bearer SUPABASE_SECRET_KEY_PLACEHOLDER"
            }
          ]
        },
        "options": {
          "response": {
            "response": {
              "fullResponse": true
            }
          }
        }
      },
      "id": "fetch-previous",
      "name": "Fetch Previous Queries",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [440, 420]
    },
    {
      "parameters": {
        "jsCode": "// Compare current vs previous week queries for rank drops and CTR anomalies\nconst current = $('Fetch Current Queries').first().json.body || [];\nconst previous = $('Fetch Previous Queries').first().json.body || [];\nconst prevMap = new Map(previous.map(p => [p.query, p]));\n\nconst urgentAlerts = [];  // SMS-worthy: drop >= 10 positions AND >= 50 impressions\nconst warnings = [];      // Weekly digest only: drop 5-9 positions\nconst ctrAnomalies = [];  // CTR drops > 30%\n\nfor (const cur of current) {\n  const prev = prevMap.get(cur.query);\n  if (!prev) continue;\n\n  const positionDrop = cur.position - prev.position; // positive = worse ranking\n\n  // Urgent: drop >= 10 positions AND >= 50 weekly impressions\n  if (positionDrop >= 10 && prev.impressions >= 50) {\n    urgentAlerts.push({\n      type: 'urgent_rank_drop',\n      query: cur.query,\n      page: cur.page,\n      oldPosition: Math.round(prev.position),\n      newPosition: Math.round(cur.position),\n      drop: Math.round(positionDrop),\n      impressions: cur.impressions\n    });\n  }\n  // Warning: drop 5-9 positions AND >= 20 impressions\n  else if (positionDrop >= 5 && prev.impressions >= 20) {\n    warnings.push({\n      type: 'rank_drop_warning',\n      query: cur.query,\n      page: cur.page,\n      oldPosition: Math.round(prev.position),\n      newPosition: Math.round(cur.position),\n      drop: Math.round(positionDrop)\n    });\n  }\n\n  // CTR anomaly: >30% drop with stable impressions (both >= 50)\n  if (prev.impressions >= 50 && cur.impressions >= 50 && prev.ctr > 0) {\n    const ctrDrop = (prev.ctr - cur.ctr) / prev.ctr;\n    if (ctrDrop > 0.30) {\n      ctrAnomalies.push({\n        type: 'ctr_anomaly',\n        query: cur.query,\n        page: cur.page,\n        oldCtr: (prev.ctr * 100).toFixed(1) + '%',\n        newCtr: (cur.ctr * 100).toFixed(1) + '%',\n        dropPct: (ctrDrop * 100).toFixed(0) + '%',\n        impressions: cur.impressions\n      });\n    }\n  }\n}\n\nreturn [{ json: {\n  urgentAlerts,\n  warnings,\n  ctrAnomalies,\n  hasUrgent: urgentAlerts.length > 0,\n  hasCtrAnomaly: ctrAnomalies.length > 0,\n  summary: {\n    queriesCompared: current.length,\n    urgentCount: urgentAlerts.length,\n    warningCount: warnings.length,\n    ctrAnomalyCount: ctrAnomalies.length\n  }\n}}];"
      },
      "id": "detect-drops",
      "name": "Detect Rank Drops + CTR Anomalies",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [700, 300]
    },
    {
      "parameters": {
        "jsCode": "// Store alert data for weekly summary aggregation by TG-102\nconst data = $json;\n\n// Calculate Monday of current week for week_start\nconst now = new Date();\nconst day = now.getDay();\nconst diff = now.getDate() - day + (day === 0 ? -6 : 1);\nconst mondayDate = new Date(now);\nmondayDate.setDate(diff);\nconst monday = mondayDate.toISOString().split('T')[0];\n\n// Calculate Sunday (week_end = monday + 6 days)\nconst sundayDate = new Date(mondayDate);\nsundayDate.setDate(sundayDate.getDate() + 6);\nconst sunday = sundayDate.toISOString().split('T')[0];\n\nreturn [{ json: {\n  ...data,\n  storePayload: {\n    week_start: monday,\n    week_end: sunday,\n    report_data: {\n      rank_drops: data.urgentAlerts,\n      warnings: data.warnings,\n      ctr_anomalies: data.ctrAnomalies,\n      summary: data.summary\n    },\n    top_losing_keywords: data.urgentAlerts.concat(data.warnings).map(a => a.query),\n    declining_pages: data.urgentAlerts.map(a => a.page).filter(Boolean)\n  }\n}}];"
      },
      "id": "prepare-store",
      "name": "Prepare Store Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [920, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://lwtmvzhwekgdxkaisfra.supabase.co/rest/v1/seo_weekly_reports",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "apikey",
              "value": "SUPABASE_SECRET_KEY_PLACEHOLDER"
            },
            {
              "name": "Authorization",
              "value": "Bearer SUPABASE_SECRET_KEY_PLACEHOLDER"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "Prefer",
              "value": "resolution=merge-duplicates,return=minimal"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.storePayload) }}"
      },
      "id": "store-alerts",
      "name": "Store Alert Data",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1140, 300]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "urgent-check",
              "leftValue": "={{ $json.hasUrgent }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "equals"
              }
            },
            {
              "id": "ctr-check",
              "leftValue": "={{ $json.hasCtrAnomaly }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "equals"
              }
            }
          ],
          "combinator": "or"
        }
      },
      "id": "if-urgent",
      "name": "Has Urgent Alerts",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [1360, 300]
    },
    {
      "parameters": {
        "jsCode": "// Build concise SMS alert for owner\nconst data = $json;\nconst lines = [];\n\nif (data.urgentAlerts.length > 0) {\n  lines.push('RANK DROPS:');\n  data.urgentAlerts.slice(0, 3).forEach(a => {\n    lines.push(`\"${a.query}\" dropped ${a.drop} pos (${a.oldPosition}->${a.newPosition})`);\n  });\n  if (data.urgentAlerts.length > 3) lines.push(`+${data.urgentAlerts.length - 3} more`);\n}\n\nif (data.ctrAnomalies.length > 0) {\n  lines.push('CTR DROPS:');\n  data.ctrAnomalies.slice(0, 2).forEach(a => {\n    lines.push(`\"${a.query}\" CTR ${a.oldCtr}->${a.newCtr}`);\n  });\n  if (data.ctrAnomalies.length > 2) lines.push(`+${data.ctrAnomalies.length - 2} more`);\n}\n\nconst message = 'TG SEO Alert\\n' + lines.join('\\n');\nreturn [{ json: {\n  to_phone: 'OWNER_PHONE_PLACEHOLDER',\n  message_body: message,\n  workflow_name: 'TG-97',\n  message_type: 'seo_alert'\n}}];"
      },
      "id": "build-sms",
      "name": "Build SMS Alert",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1580, 200]
    },
    {
      "parameters": {
        "workflowId": "AprqI2DgQA8lehij"
      },
      "id": "send-sms",
      "name": "Send SMS via TG-94",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.1,
      "position": [1800, 200]
    }
  ],
  "connections": {
    "Execute Workflow Trigger": {
      "main": [
        [{ "node": "Calculate Date Range", "type": "main", "index": 0 }]
      ]
    },
    "Calculate Date Range": {
      "main": [
        [
          { "node": "Fetch Current Queries", "type": "main", "index": 0 },
          { "node": "Fetch Previous Queries", "type": "main", "index": 0 }
        ]
      ]
    },
    "Fetch Current Queries": {
      "main": [
        [{ "node": "Detect Rank Drops + CTR Anomalies", "type": "main", "index": 0 }]
      ]
    },
    "Fetch Previous Queries": {
      "main": [
        [{ "node": "Detect Rank Drops + CTR Anomalies", "type": "main", "index": 0 }]
      ]
    },
    "Detect Rank Drops + CTR Anomalies": {
      "main": [
        [{ "node": "Prepare Store Payload", "type": "main", "index": 0 }]
      ]
    },
    "Prepare Store Payload": {
      "main": [
        [{ "node": "Store Alert Data", "type": "main", "index": 0 }]
      ]
    },
    "Store Alert Data": {
      "main": [
        [{ "node": "Has Urgent Alerts", "type": "main", "index": 0 }]
      ]
    },
    "Has Urgent Alerts": {
      "main": [
        [{ "node": "Build SMS Alert", "type": "main", "index": 0 }],
        []
      ]
    },
    "Build SMS Alert": {
      "main": [
        [{ "node": "Send SMS via TG-94", "type": "main", "index": 0 }]
      ]
    }
  },
  "settings": {
    "availableInMCP": true
  }
}